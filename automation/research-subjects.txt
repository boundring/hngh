log-presentation patterns
wiki-viewer QoL comparison
tech-tree research UX precedents
session-cost display formats
gantt legibility patterns
telemetry schema exemplars
self-funding paths (publications/ebook/site -> operator runway)
unattended plan authoring safety: operator-accepted machine-drafted plans
alert-to-work routing patterns: closing the self-observation loop
search-grounded research beats: web search, reference capture, source quality
delegation lane parallelism: multi-lane omp-bridge sessions and queueing
ceremony-cost reduction: batching kernel doc landings safely
govbench-ci-evidence	What CI/CD systems publish machine-checkable evidence of change governance, and what do their artifacts cover?
govbench-weak-validation-baseline	What is the weakest validation baseline a governance benchmark must compare against (rubber-stamp approval, unchecked auto-merge)?
govbench-voting-prior-art	What prior art exists for procedural voting on changes (sign-offs, LGTM counts, quorum rules) and how is it scored?
govbench-metrics-v1	Which metrics make tamper-evidence, approved=executed, reconstruction-from-record, and refusal-accounting measurable?
govbench-scenario-corpus	What scenario corpus (benign, adversarial, partial-failure changes) exercises a governance benchmark honestly?
govbench-adapter-contract	What adapter contract lets different change-governance systems run the same governance benchmark unmodified?
ctx-compaction-strategies	Which long-session compaction strategies (checkpointing state at turn boundaries) let a reborn session reorient in one bounded read without losing verified facts?
ctx-structured-briefs	Which structured brief fields measurably cut reorientation tokens after a session death, and which fields are noise?
ctx-retrieval-vs-repetition	At equal task quality, when do orientation pointers (retrieval) beat inlined content (repetition) for session input cost, and how does the 1500-byte pack cap trade off?
ctx-ttsr-scope-gaps	What session-discipline violations escape stream-rule scopes (thinking, subagents) and how should record-level screening close them?

ainglish-engagement	Explore ainglish.org keyless surfaces (246 proposals, preflight screening, stage-history ledgers) — identify one proposal worth seconding and test the preflight API for Hngh evidence-gathering.
cistern-architecture-review	Review the Cistern emacs game's clean architecture (17K LOC; domain/game/view/input/driver layers, one state object threaded everywhere) against Hngh's own core/edge doctrine — which patterns transfer, which diverge?
cistern-roadmap-deferred	The Cistern roadmap carries DEFERRED items (DESIGN-SPEC §6, resolved outside the roadmap) — what are they, which are actionable now, and which would Hngh's machinery (disposition spine, research beats, context packs) contribute to?
cistern-test-coverage	Cistern's suite is a custom batch runner (tests/run.el, cistern-run-all-tests, ~139 entries) not ERT — is it fully headless batch-runnable per R9 conformance, and what coverage gaps exist (view render, input adapter, driver timers)?
synth-2026-09-08-1	How does Bernstein's "CI re-verifies the committed receipt on every push to main" mechanism map onto Hngh's current ledger-sync commit race, and can a similar CI gate be added to prevent published proofs from rotting?
synth-2026-09-10-3	How should the ctx-compaction-strategies findings (fact ledger, write-time-bounded Tier 0, freshness validation) be implemented against the demonstrated hngh failure mode, and what are the specific codebase integration points for these design constraints?
synth-2026-09-10-1	How does Bernstein's "CI re-verifies the committed receipt on every push to main" mechanism map onto Hngh's current ledger-sync commit race, and what specific guardrail prevents published proofs from rotting in the unversioned plan ledger?
fail-20260911-system-network-down	Why does the oversight system-network-down flag fire nightly while push/ping/GitHub keep working — what does the headroom predicate actually measure, and which probe makes it honest?
fail-20260911-overnight-plan-accept-gate-kernel	Why is the kernel gate (make test rc=2) red for two days and what exact commits break the loop-history guard — can the miss be declared and re-certified through the loop without rewriting history?
acpkernel-boundary-model	ACP kernel decoupling prior art: what does acp-kernel's boundary model (one injected port, state-in/state-out, DESIGN.md s5) teach hngh's port enforcement?
gdelt-gkg-trends	GDELT GKG deeper features: which theme-timeline deltas, V2Counts enrichments, and multi-window NumSources aggregation would measurably improve the morning paper's story selection (docs/records/2026-09-12-gdelt-source.md deferrals)?
acpkernel-process-track	What does a solo author's issue-RFC + self-merge + release-PR cadence (acp-kernel #166/#167/#168, release PRs #240/#243/#246) teach hngh's public design visibility and backlog-disposition practice?
os-harness-distro-packaging	What distro packaging prior art (PKGBUILD/AUR, deb/rpm packaging, Nix/system-manager-style declarative layers) should hngh's environment contract and package registry rungs learn from?
os-harness-systemd-integration	What depth of systemd integration (user units, generators, timer-vs-unit tradeoffs) makes an OS-harness harness cleanly alongside a host systemd rather than fighting it?
os-harness-cross-platform-patterns	What patterns let one harness core late-bind to different DE/WM (KDE/GNOME/X11), editors, and browsers without hardcoding per-platform edges?
bigeye-caution-audit	What does suanrongqieqiezi/bigeye actually contain when its Chinese docs are translated and audited (persona-overwrite claims, unlicensed binaries, committed credential-format config), and what collection policy should govern unknown-provenance Chinese-language repos for hngh?
harness-delegation-patterns	How do OpenSandbox's contract-first ports, Multica's task-barrier daemon version-follow and worktree GC, and jcode's mode-gated spawn depth and single-writer plan slot compare to hngh's worker/ceremony architecture, and which isolation and supervision patterns transfer to a single-host self-governing kernel?
fail-20260907-slow-unit-dropin-50-research-overflow.sh	Why does cadence/30m/50-research-overflow.sh show wall=258.9s against a 0.0s median (×6), is that latency a defect or expected model-leg work, and what disposition (fix or park) does the beat design support?
fail-20260912-field-survey-literature-collection	What open-source data collection methods (libgen/sci-hub-class corpora, web archives, RSS/API aggregation) can gather supporting and detracting literature for hngh research lines in the wild, and what provenance/licensing guardrails must such collection obey?
fail-20260912-serious-manga-narration-register	What do A Drifting Life-class works (gekiga autobiography) teach about deadpan narration and craft-persistence humor, and how does that narration register distill into hngh's comedy banks (manga-draft.py NARRATIONS, dialogue templates) as flat declarative lines of the machine's small dramas played straight?
gdelt-webngrams-reconstruction	GDELT Web News NGrams 3.0 (3.6 MB gz/minute JSON-NL, 2020-present): which targeted minute-file fetch + phrase-surge counter + overlap-merge reconstruction slice (prior art iandreafc/gdeltnews, doi 10.3390/bdcc10020045) would measurably add dramatization feedstock and trend receipts to the manga/newspaper lanes at hobby compute?
manga-panel-conventions	What manga panel grammar (reading order, bubble/caption placement, SFX typography, screentone conventions) should hngh's panel skeleton and multi-panel layouts follow for a gag newspaper format?
dramatization-plot-taxonomy	Which comedy dramatization plot types (incongruity, escalation, reversal, slapstick, deadpan) map cleanly onto CAMEO event lanes (conflict/cooperation/aid/yield) for procedural gag generation, with what attribution-safety boundaries?
humor-development	Which deadpan craft-narration moves (Tatsumi A Drifting Life mode: the mundane played absolutely straight, wit from restraint) should the house voice adopt per lane (lib/quips.py quip lanes, generated-article closers), and what register-law boundary keeps the dryness from mocking the subject?
policy-lift-refactor-design	Which automation-tier policies (failfirst speed state machine, causes classification table, supervision triage thresholds, model/cost routing) lift into hngh.domain/application as pure fixture-tested use cases per the charter's policy law, and which mechanisms stay behind ports (agent-supervision classify() and kernel select-course as the in-repo lift pattern)?
transaction-certificate-system-mutations	What certificate shape lets one certificate cover a multi-dependency system mutation (one action over a closed artifact set with an aggregate hash, git-commit-shaped) so Rung D package/config operations ride the existing one-action-per-certificate mutation adapter without a blank permission slip or a daemon?
fail-20260912-For-born-PDF-scholarly-corpora-where-ups	For born-PDF scholarly corpora where upstream structure capture is impossible, what post-hoc extraction pipeline (GROBID/TEI-class) meets R1's anchor-and-hierarchy pass/fail criterion?
fail-20260912-What-audit-sampling-and-uncertainty-tier	What audit sampling and uncertainty tiering should gate OpenAlex/Crossref OA-status fields before they are trusted to enforce the R3 two-class license split at ingest?
patrol-20260912-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
manga-study-syllabus	Historically significant chapters/pages per operator-local title (Blame! ch1 and the Safeguard chase, Astro Boy origin chapters, Phoenix dawn/future volumes, Ping Pong ch1-5 and the Kaoru meditations, Dai Dark ch1 gag-embedded-in-grimness pacing, Dorohedoro's dense world-building and morbid-gag timing when the collection lands) -- a study syllabus for the manga-style-research reading line, cited per title/chapter.
manga-layout-metrics	What published panel-grammar analysis (panel-per-page density, gutter rhythm, screentone coverage) exists for Tezuka/Nihei/Matsumoto/Hayashida, and do the 2026-09-12 local layout metrics (docs/research/2026-09-12-manga-collection-study.md) match or contradict the critical literature?
patrol-20260912-email-send-failed	patrol: surface email filed send-failed on two consecutive runs -- why does it keep failing and which guardrail closes it?
visual-storytelling-principles	What visual-storytelling principles (McCloud's closure/transition taxonomy, Eisner's sequential framing, panel-to-panel moment-to-moment vs action-to-action transitions) map onto the vision pipeline's panel_flow/imagery_carries fields, and which transitions should manga-draft.py beats cycle through for image-only narrative register?
narrative-structure-styles	Which narrative-structure styles (kishotenketsu four-beat, Western three-act, serialized cliffhook rhythm) best describe manga chapter-transition pacing, and can the vision lessons' page_turn_hook distribution (cliff vs quiet beat) be trained to detect the style per title?
story-beat-taxonomy	Which story-beat taxonomy (save-the-cat beats, Haruhi-style scene beats, or comic-specific action-beat/reaction-beat pairs) should annotate manga-draft.py's beat structure, and how does each beat class map to narrative_mode (image-only/dialogue-only/mixed) ratios measured by jobs/manga-vision.py?
fail-20260912-Does-lib-quips-py-expose-any-existing-te	Does lib/quips.py expose any existing template-validation or rendering hook where a wink-marker lexical guard could land as configuration rather than new infrastructure?
fail-20260912-Does-the-hngh-kernel-emit-user-facing-st	Does the hngh kernel emit user-facing strings (error messages, status output, commit trailers) that constitute a third voice lane requiring assessment against the four moves and the register-law?
patrol-20260913-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260913-How-does-the-presence-of-multiple-confli	How does the presence of multiple conflicting license declarations within a single Crossref record (e.g., different licenses for different content versions) impact the T1 tier assessment and the required audit sampling rate?
fail-20260913-What-is-the-minimum-viable-set-of-automa	What is the minimum viable set of automatable checks (T0/T1) that can be performed at ingest time to reduce the manual adjudication load for the R3 audit gate without compromising the false-permissive error bound?
fail-20260913-Does-the-CLI-entry-point-bin-hngh-or-lib	Does the CLI entry point (`bin/hngh`) or `lib/automation.py` expose a global logging filter or output sanitizer where a wink-marker guard could be applied without modifying `lib/quips.py` itself?
fail-20260913-Is-there-an-existing-pattern-in-the-repo	Is there an existing pattern in the repository for lexical guards or text sanitization (e.g., in `lib/logs.py` or `lib/display.py`) that could be reused for quips?
fail-20260913-Does-the-hngh-kernel-write-any-commit-tr	Does the hngh kernel write any commit trailers (attribution, provenance, automation markers), and if so, what is their exact schema as authored ledger content?
fail-20260913-Can-lint-level-checks-mechanically-flag-	Can lint-level checks mechanically flag unattributed async status emissions and unmarked trailers at authoring time, and at what false-positive cost?
patrol-20260913-email-send-failed	patrol: surface email filed send-failed on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260913-Does-the-hngh-kernel-s-component-registr	Does the `hngh` kernel's component registry support machine-readable generation from source code, and if so, can a lint check validate literal sources against this generated artifact without manual maintenance?
fail-20260913-Can-the-Trailer-Completeness-check-be-ex	Can the "Trailer Completeness" check be extended to handle helper-delegated terminal emissions by analyzing call graphs within `hngh-automation`, or is interprocedural analysis required for accurate false-positive suppression?
fail-20260913-Does-lib-automation-py-invoke-bin-hngh-v	Does `lib/automation.py` invoke `bin/hngh` via `subprocess` or direct module import?
fail-20260913-Is-the-upstream-guardrail-bug-filing-sco	Is the upstream guardrail bug filing scoped to `lib/quips.py` specifically or applied repo-wide?
fail-20260913-Does-the-bin-hngh-executable-have-a-stab	Does the `bin/hngh` executable have a stable CLI interface contract (e.g., version flag, JSON output mode) that can be pinned in capture-and-replay fixtures?
fail-20260913-Are-there-any-existing-integration-tests	Are there any existing integration tests or CI pipelines that already exercise the `lib/automation.py`  `bin/hngh` boundary, and do they pass with the current implementation?
fail-20260913-Has-the-upstream-guardrail-bug-from-2026	Has the upstream guardrail bug from 2026-08-25 been resolved in the current kernel version, and does `lib/automation.py` now handle the corrected CLI contract without errors?
fail-20260913-Can-the-existing-overnight-harness-be-re	Can the existing overnight harness be refactored into a fast, synchronous CI job that runs in under 2 minutes to serve as a pre-merge gate for the `lib/automation.py`  `bin/hngh` boundary?
fail-20260909-slow-unit-dropin-33-research-beat.sh	Why does cadence/hour/33-research-beat.sh show wall=28.3s against a 0.1s median (x9, later 387.1s vs 174.6s median), is that latency a defect or expected model-leg work, and what disposition (fix or park) does the beat design support?
fail-20260913-Does-the-deployed-kernel-version-on-idle	Does the deployed kernel version on idle hosts match the latest tag in `~/Projects/etc/hngh` that contains the guardrail fix commit?
fail-20260913-Are-there-multiple-CLI-invocation-sites-	Are there multiple CLI invocation sites in `lib/automation.py`, and do all of them conform to the corrected contract defined in the kernel's fix commit?
fail-20260909-wake-mutation-lane-src-mutation	Can the wake-mutation-lane :wake-mutation kernel src mutation be fixed by a machine session, or is the operator boundary (2026-09-03 staging plan) the correct terminal disposition for the parked rotation beat?
patrol-20260913-gate-cure-gate-cure-refused	patrol: surface gate-cure filed gate-cure-refused on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260913-Does-reading-cadence-hour-33-research-be	Does reading cadence/hour/33-research-beat.sh confirm that wall_s wraps the model call and that no timeout/max_tokens guard exists, closing OT1?
fail-20260913-Do-sibling-scripts-under-cadence-hour-sh	Do sibling scripts under cadence/hour/ share the synchronous unguarded model-leg pattern, such that the fix should ship as a shared async wrapper rather than a one-off edit (OT3)?
patrol-20260914-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260914-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260914-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260914-Does-grepping-cadence-hour-33-research-b	Does grepping cadence/hour/33-research-beat.sh for `timeout|max_tokens|SECONDS|date +%s` close OT1 affirmatively and reveal where wall_s is assigned relative to the model invocation?
fail-20260914-Should-the-beat-orchestrator-write-a-tru	Should the beat orchestrator write a `truncated: true` (finish_reason) header into crystallized docs when completion hits the max_tokens cap, mirroring the proposed `timed_out: true` sentinel for wall_s?
fail-20260910-overnight-bridge-refused-2026-09-06-routed-system-network-down	Why did overnight beat 2026-09-06-routed-system-network-down refuse its bridge run with conflict labels=record-conflict, and which guardrail closes the class?
fail-20260910-overnight-plan-accept-blocked-2026-09-09-stall-recovery-and-operator-surfaces	Why did plan 2026-09-09-stall-recovery-and-operator-surfaces fail auto-accept twice with "step 1 has no Verification line", and is the acceptance blocker still open or already resolved by the plan acceptance and step-1 audit-close?
fail-20260910-overnight-plan-accept-gate-kernel	Why did plan acceptance fail with kernel make test rc=2 for identity overnight:plan-accept-gate:kernel, and is the blocker still open or already resolved?
fail-20260910-slow-unit-dropin-16-remote-push.sh	Why does cadence/hour/16-remote-push.sh show wall=23.1s (later 28.6-40.8s) against a 0.0s median (x12+), is that latency a defect or expected gate-rerun work, and what disposition (fix or park) does the beat design support?
fail-20260910-push-blocked-openrouter-key-hngh	Why did GitHub push protection decline origin/main pushes carrying the OpenRouter key in docs/research/2026-09-10-lobehub-api-research.md:11,81, and is the block cleared now?
patrol-20260914-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260911-correction-style-css	Why did the feedback-apply beat file alert identity correction-style-css ("style.css missing or unparseable; quick item t1 left unapplied" x2) on 2026-09-11, does the condition still reproduce, and what disposition (fix or park) closes it?
fail-20260911-ui-audit-axe-aria-required-children	Why does the ui-audit axe scan report aria-required-children on #tabs (role=tablist) when the static HTML carries nine role=tab children, which element is the disallowed child, and what disposition (fix or park) closes the alert that re-occurred across 2026-09-11..09-14?
fail-20260912-correction-3146c023	Why did clicking "mark read" on the dashboard report queue appear to do nothing for reports (76 aggregated occurrences by 2026-09-12T19:18Z), and what disposition (fix or park) closes it?
fail-20260914-Does-the-github-ci-workflow-definition-c	Does the github-ci workflow definition contain an embedded copy of the patrol verdict rule, and does it diff against the kernel's canonical rules file?
fail-20260914-Does-the-kernel-s-verdict-taxonomy-alrea	Does the kernel's verdict taxonomy already distinguish infrastructure failure from agent bad-execution, and which file defines the verdict classes and their budget/counter effects?
fail-20260912-correction-407311fb	Why did feedback-apply correction item 407311fb land as "no named check found" (alert identity correction-407311fb, x79 at reports.md:2310) for an operator prose paste ("we are copying and pasting the parts of this page that are not being displayed correctly" -> wake-mutatio...), and what disposition (fix or park) closes it?
fail-20260912-sn-627926	What makes the feedback correction fail-20260912-sn-627926 (links on the dashboard Plans page 404) actionable, and what smallest ROOT-CAUSE fix addresses it?
fail-20260912-correction-b61fed0f	Why does the sessions observatory (dashboard tab "Sessions · observatory") not scroll on either side — the session list (left) and the transcript detail (right) — per operator correction b61fed0f "Scrolling seems to be broken, both sides.", and what disposition (fix or park) closes it?
fail-20260914-Does-the-current-hngh-journal-error-sche	Does the current `hngh` journal-error schema include a mandatory `waiver_metadata` object, and if so, what are the required fields for expiry and recheck conditions?
fail-20260914-How-does-the-run-close-scoring-logic-in-	How does the run-close scoring logic in `hngh-automation` currently handle errors that are filed but lack a `claimed` or `waived` status flag?
fail-20260912-deck-unreachable-2026-09-12	Why did the deck facts pull flip from reachable (00:01Z and 01:01Z facts landed 2026-09-12) to ssh probe rc=255 unreachable alerts for the rest of that UTC day, and what disposition (fix or park) closes alert identity deck-unreachable-2026-09-12?
fail-20260912-overnight-plan-accept-gate-kernel	Was the 2026-09-12 kernel make test rc=2 that blocked routed-plan acceptance a real kernel defect or a load transient, and what disposition (fix or park) closes alert identity overnight:plan-accept-gate:kernel?
fail-20260912-patrol-automation-gate	Why did patrol fire gate-stale "no gate crumb found" on hngh-automation (routed 2026-09-12T21:03Z), and what disposition (fix or park) closes alert identity patrol:automation-gate?
fail-20260912-patrol-paper	Why did patrol alert identity patrol:paper fire deck-a-empty exactly once on digest/2026-09-12.md at 2026-09-12T20:03Z ("29 blocks, 0 items") when the same file passed with 142 items 20 seconds later, and what disposition (fix or park) closes it?
fail-20260914-Does-the-reaction-class-need-to-be-split	Does the `reaction` class need to be split into `reaction-internal` and `reaction-external` to satisfy the "closed vocabulary" constraint in R1?
fail-20260914-Can-jobs-manga-vision-py-be-extended-to-	Can `jobs/manga-vision.py` be extended to output per-beat-class confidence scores, allowing us to detect mislabeled beats automatically during the T1 measurement run?
fail-20260912-slow-unit-dropin-33-research-beat.sh	Why does dropin:33-research-beat.sh keep re-firing slow-unit rows (wall=137.4s median=0.2s x10 on 09-12, wall=552.5s median=108.9s x10 on 09-13; identity re-routed 5x since 2026-09-08), is that latency a defect or by-design bimodality, and what disposition (fix or park) does the evidence support?
fail-20260912-slow-unit-dropin-20-workbeat.sh	Why does dropin:20-workbeat.sh show wall=1964.6s against a 40.3s median (x6, routed 2026-09-12T20:00:35Z), is that latency a defect or expected forethought-dream+plan-session work, and what disposition (fix or park) does the evidence support?
fail-20260914-Does-the-acceptance-parser-source-in-hng	Does the acceptance parser source in hngh/hngh-automation contain a hardcoded "has no Verification line" rejection, and does it re-validate on every auto-accept pass regardless of prior operator overrides?
fail-20260914-Which-artifact-records-the-plan-acceptan	Which artifact records the plan-acceptance event for 2026-09-09-stall-recovery-and-operator-surfaces, and does it distinguish parse_pass from operator override as R2 requires?
fail-20260914-system-low-mem	Why did system-awareness set the low-mem headroom flag at 2026-09-14T02:00:16Z (mem >= LOW_MEM_PCT 90, alert identity system-low-mem routed 2026-09-14T02:00:20Z), was it a persistent memory defect or a transient spike, and what disposition (fix or park) does the evidence support?
fail-20260914-Does-the-ui-audit-pipeline-persist-axe-c	Does the ui-audit pipeline persist axe-core JSON output to an `artifacts/` directory, and if so, what is the CSS selector in `nodes[0].target` for the `aria-required-children` violation on `#tabs`?
fail-20260914-Is-the-tabs-element-scanned-as-static-HT	Is the `#tabs` element scanned as static HTML or live DOM, and does the hydration process inject any non-tab nodes (e.g., tooltips, focus traps) into the `tablist` container?
fail-20260914-Does-the-backend-log-show-76-successful-	Does the backend log show 76 successful `200 OK` responses for `mark-read` requests that failed to persist, or 76 client-side errors?
fail-20260914-Is-there-a-race-condition-where-rapid-su	Is there a race condition where rapid successive clicks on "mark read" trigger an idempotency guard that silently drops subsequent writes?
fail-20260914-push-divergence-jcode-20260914	Why did the 2026-09-14T19:00Z push land non-fast-forward (2nd refusal), and what coordination rule prevents two same-host jcode sessions from racing origin main?
fail-20260915-Does-the-beat-script-s-state-model-disti	Does the beat script's state model distinguish between "timeout-complete" and "in-progress" markers, and if not, what is the minimal patch to close this gap?
fail-20260915-Is-the-identity-re-routing-logic-in-drop	Is the identity re-routing logic in `dropin:33-research-beat.sh` scoped to a stable key that survives re-routes, or does it invalidate per-identity completion state?
fail-20260915-Does-the-research-lines-tsv-schema-inclu	Does the `research-lines.tsv` schema include a field that explicitly maps each state (e.g., `planned`, `expanding`) to a corresponding page-rendering template or output path?
fail-20260915-Are-there-any-existing-logs-or-error-tra	Are there any existing logs or error traces in the nightly cycle orchestrator that indicate file-write conflicts or skipped render steps for lines in the `planned` state during the last 7 days?
patrol-20260915-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260915-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260915-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260915-notify-email-refire	notify-email sent 48 same-day mails with the top alerts repeating (bench-trigger x4, stall-recovery x4, peer-hardening x3) — why does the IMMEDIATE classifier re-fire repeats (dedup absent?), and should repeat identities route digest-only after first send?
patrol-20260915-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260915-Do-the-digest-RESEARCH-BEAT-files-for-th	Do the digest/RESEARCH-BEAT-* files for this line contain any raw log excerpts of the 76 mark-read responses, and if so what status codes do they actually show?
fail-20260915-After-re-running-the-truncated-final-bea	After re-running the truncated final beat, does the completed synthesis confirm or retract the optimistic-acknowledgment hypothesis with quoted code from hngh/src/api?
fail-20260915-Has-R1-systemctl-status-list-timers-jour	Has R1 (systemctl status / list-timers / journalctl capture) been executed and persisted to the repo since crystallization on 2026-09-15, and if not, what is the escalation path for an unexecuted verification recommendation?
fail-20260915-Where-is-the-crumb-writer-s-cadence-inte	Where is the crumb writer's cadence intent currently recorded (unit file, config, or nowhere), and can a single `now - last_success > cadence` staleness check be implemented against R3's self-persisting timestamp log?
fail-20260915-What-are-the-exact-file-paths-for-the-ca	What are the exact file paths for the canonical verdict rule in the kernel repo and the embedded copy in the hngh-automation CI workflow?
fail-20260915-Are-there-additional-surfaces-test-fixtu	Are there additional surfaces (test fixtures, docs, other repos) containing copies of the patrol verdict rule beyond the two identified in the drift note?
fail-20260915-Does-the-current-reaction-class-implemen	Does the current `reaction` class implementation in `~/Projects/etc/hngh/src/reaction.rs` (or equivalent) contain paths that produce unbounded or dynamically named outputs, which would necessitate a structural split under R1?
fail-20260915-What-is-the-verbatim-text-of-constraint-	What is the verbatim text of constraint R1 in the hngh kernel specification, and does it explicitly extend "closed vocabulary" to externally observable reaction effects beyond internal state transitions?
patrol-20260915-journal-error-unit-not-practiced	patrol: surface journal-error filed unit-not-practiced on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260915-If-drift-is-confirmed-in-the-scroll-beha	If drift is confirmed in the scroll behavior, which other nominally-shared dashboard behaviors (e.g. tab-state persistence, cost display per `2026-08-28-session-cost-display.md`) exhibit the same duplicated-and-diverged pattern?
fail-20260915-Can-the-hngh-component-map-SRC-2026-08-2	Can the hngh component map (SRC-2026-08-24-027) be regenerated mechanically from the tab registry at build time, turning a staling artifact into a continuously verified one?
fail-20260915-Does-the-hngh-automation-provisioning-te	Does the hngh-automation provisioning template set `Storage=persistent` for journald and enable sysstat collection, or are test hosts ephemeral containers that destroy all three telemetry classes at teardown?
fail-20260915-If-R1-confirms-1-surviving-class-on-1-ho	If R1 confirms 1 surviving class on 1 host, does the OOM-killer log signature in the hngh kernel match mainline `mm/oom_kill.c` format strings, or has the path been restructured such that the R3 grep patterns require modification?
fail-20260915-Does-the-llm-wiki-invariant-coordination	Does the llm-wiki invariant-coordination note (obs-2026-08-24) already specify a same-host push rule whose enforcement gap  not absence  explains the 2026-09-14 race?
fail-20260915-Does-the-git-reflog-for-origin-main-arou	Does the git reflog for origin/main around 2026-09-14T19:00Z show two same-host sessions interleaving pushes, confirming F1's attribution empirically?
fail-20260915-FOLLOWON	FOLLOWON:
fail-20260915-Does-the-hngh-TUI-render-loop-use-altern	Does the `hngh` TUI render loop use alternate-screen mode, and if so, does it implement a "last K rows" windowing slice before drawing?
fail-20260915-On-host-touch-which-exact-file-and-line-	On host touch, which exact file and line in ~/Projects/etc/hngh consumes the make exit code, and is it a `set -e` abort or an explicit status branch?
fail-20260915-What-make-version-does-the-repo-s-toolch	What make version does the repo's toolchain use, and does its failure-line format match `make: *** [target] Error N` for reliable failing-target extraction?
fail-20260915-Does-the-file-structure-at-home-bricker-	Does the file structure at ~/Projects/etc/hngh contain a JSON or YAML record with keys `parse_pass` and `operator_override` for the run tag 2026-09-09-stall-recovery-and-operator-surfaces?
fail-20260915-If-no-structured-artifact-exists-does-th	If no structured artifact exists, does the source note [[sources/hngh-2026-09-09-stall-lessons]] contain prose that explicitly describes a failure mode where parse_pass was true but operator_override was false?
patrol-20260915-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260915-Does-the-hngh-kernels-reconciliation-loo	Does the hngh kernels reconciliation loop currently handle a "timeout_complete" state by triggering a retry, or does it require a new handler branch?
fail-20260915-Are-there-existing-tests-in-the-reposito	Are there existing tests in the repository that assert on the distinction between clean-complete and timeout-complete states, or is this gap untested?
fail-20260915-What-is-the-exact-observable-terminal-co	What is the exact observable terminal contract (exit code, status file, or log line) that the hngh kernel produces for both clean-complete and timeout-complete states?
fail-20260915-Which-specific-environment-variables-or-	Which specific environment variables or binary paths constitute the "subprocess-stub-seam" documented in the vault pointer for hermetic testing?
patrol-20260915-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260915-Does-the-obs-2026-08-25-hngh-automation-	Does the `obs-2026-08-25-hngh-automation-overnight-harness-built-verified-enabled` file contain the grep output for `Storage=persistent` in the journald configuration template?
fail-20260915-Does-the-hngh-automation-teardown-script	Does the hngh-automation teardown script explicitly call `journalctl --vacuum-size=0` or `rm -rf /var/log/journal` before container destruction, or does it rely solely on filesystem discard?
fail-20260915-What-does-a-grep-for-timeout-complete-in	What does a grep for `timeout_complete` in the hngh kernel repository reveal about the current state dispatch logic?
fail-20260915-Where-does-the-hngh-reconciliation-loop-	Where does the hngh reconciliation loop persist its state, and is that storage mechanism resilient to multi-process CLI execution?
patrol-20260916-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260916-Does-the-file-sources-obs-2026-08-25-hng	Does the file `sources/obs-2026-08-25-hngh-automation-overnight-harness-built-verified-enabled` contain the literal string `Storage=persistent`?
fail-20260916-Is-there-a-verified-file-path-under-home	Is there a verified file path under `~/Projects/etc/hngh` that defines a journald configuration template with `Storage=persistent`?
fail-20260916-Does-the-hngh-kernel-s-state-machine-def	Does the hngh kernel's state machine definition file explicitly list the valid states for `research-lines.tsv`, and does that list match the domain enumerated in R2 Step 2?
fail-20260916-If-answer-b-holds-is-there-a-CI-check-th	If answer (b) holds, is there a CI check that validates the state strings in `research-lines.tsv` against the hard-coded state literals in the kernel renderer to prevent drift?
patrol-20260916-email-send-failed	patrol: surface email filed send-failed on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260916-Does-the-hngh-hngh-automation-directory-	Does the `hngh/hngh-automation` directory exist in the kernel repository, and if so, does it contain a pre-defined capture recipe for R1?
fail-20260916-What-is-the-exact-systemd-unit-name-e-g-	What is the exact systemd unit name (e.g., `hngh.service`, `hngh-timer.timer`) required to execute the R1 verification commands?
fail-20260916-Does-the-2026-08-28-session-cost-display	Does the `2026-08-28-session-cost-display.md` intent document specify a single canonical formatting function or module path that both dashboard surfaces should import?
fail-20260916-Can-the-verdict-rule-drift-two-surfaces-	Can the `verdict-rule-drift-two-surfaces` prior-art case be traced to a specific commit where the duplication was introduced, revealing if it was an intentional copy-paste or an accidental refactor?
fail-20260916-Does-the-hngh-kernel-retain-any-mainline	Does the hngh kernel retain any mainline `mm/oom_kill.c` code paths, and if so, are they reachable in the build configuration?
fail-20260916-What-is-the-exact-format-string-used-by-	What is the exact format string used by the hngh OOM handler, as extracted from source via `grep -rn "Out of memory" ~/Projects/etc/hngh --include='*.c'`?
fail-20260916-Does-the-hngh-automation-CI-workflow-cur	Does the hngh-automation CI workflow currently import the kernel verdict logic or duplicate it inline?
fail-20260916-Is-there-a-VERDICT-RULE-VERSION-constant	Is there a `VERDICT_RULE_VERSION` constant defined in the kernel repo to facilitate drift detection?
patrol-20260916-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260916-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260916-Does-the-hngh-repository-s-CI-configurat	Does the `hngh` repository's CI configuration (e.g., `.github/workflows/*.yml`) explicitly pin a specific version of `make` or use a container image with a known Make version?
fail-20260916-Are-there-any-captured-build-logs-from-h	Are there any captured build logs from `hngh`'s CI runs available in the repository (e.g., in an `artifacts/` or `logs/` directory) that demonstrate actual recursive make error lines?
fail-20260916-Does-the-vault-pointer-for-subprocess-st	Does the vault pointer for "subprocess-stub-seam" actually exist in `/path/to/llm-wiki/vault/sources/`, and if so, what is its exact file path?
fail-20260916-Do-any-files-in-home-bricker-Projects-et	Do any files in `~/Projects/etc/hngh` contain the strings `LD_PRELOAD`, `getenv("HNNGH_")`, or a directory named `stubs/`?
fail-20260916-Does-sources-SRC-2026-08-18-003-explicit	Does `[[sources/SRC-2026-08-18-003]]` explicitly name a required artifact path or verification step for build logs, or is reproducibility framed only as a general principle?
fail-20260916-Which-specific-SLSA-level-e-g-Level-1-2-	Which specific SLSA level (e.g., Level 1, 2, or 3) in `[[sources/SRC-2026-08-24-006]]` mandates the retention of build logs as a hard compliance gate for hngh?
fail-20260916-Does-the-file-sources-obs-2026-08-25-hng	Does the file `sources/obs-2026-08-25-hngh-automation-overnight-harness-built-verified-enabled` exist in the vault at all?
fail-20260916-What-is-the-exact-exit-status-and-match-	What is the exact exit status and match count of running `LC_ALL=C grep -F -- 'Storage=persistent' sources/obs-2026-08-25-hngh-automation-overnight-harness-built-verified-enabled`?
fail-20260916-Does-the-hngh-automation-harness-include	Does the `hngh-automation` harness include a config-management surface (e.g., Ansible, Puppet, or custom scripts) that renders journald templates, making the static file path check moot?
fail-20260916-If-the-probe-returns-a-clean-negative-fo	If the probe returns a clean negative for `~/Projects/etc/hngh`, has the research root been renamed to a different path (e.g., `~/Projects/hngh`) or moved to a remote host accessible via the `remote-access-patterns` 
fail-20260916-Does-the-harness-verification-script-hng	Does the harness verification script (`hngh-automation/verify.sh` or equivalent) already include a `grep -qF 'Storage=persistent'` check that would make the external audit redundant?
fail-20260916-Is-there-a-machine-readable-sidecar-file	Is there a machine-readable sidecar file (e.g., `state.json`) for the obs artifact that records the storage mode, allowing verification via JSON parsing instead of substring matching?
fail-20260916-Does-the-CI-CD-pipeline-configuration-e-	Does the CI/CD pipeline configuration (e.g., `.github/workflows`, `Jenkinsfile`, or `Makefile`) explicitly invoke `hngh-automation/verify.sh` and treat its exit code as a build failure?
fail-20260916-Is-there-a-distinct-deployment-or-instal	Is there a distinct "deployment" or "install" step in the harness that generates or copies the storage configuration to a final path, and does any verification occur *after* this step?
patrol-20260917-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260917-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260917-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260917-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260917-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260917-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain an `install.sh` or Makefile target that explicitly writes to `/etc/systemd/system/`, and if so, what unit names does it emit?
fail-20260917-Is-there-a-canonical-definition-of-R1-in	Is there a canonical definition of "R1" in any `hngh-automation` documentation or CI/CD pipeline configuration (e.g., `.github/workflows/r1.yml`) that specifies the exact verification commands?
fail-20260917-Does-the-hngh-build-policy-explicitly-bi	Does the `hngh` build policy explicitly bind its internal log-retention gate to SLSA Level 2 or higher, and if so, is that binding documented in a compliance file?
fail-20260917-Can-the-signed-in-toto-provenance-statem	Can the signed in-toto provenance statement generated by `hngh`'s CI pipeline be used as the sole evidence for satisfying both the internal log-retention gate and external SLSA conformance?
fail-20260917-Does-the-in-toto-v1-schema-support-a-pre	Does the `in-toto` v1 schema support a `predicateType` that explicitly encodes retention policy metadata, or is sidecar logging the only standard-compliant approach?
fail-20260917-Can-a-single-Merkle-tree-root-hash-serve	Can a single Merkle tree root hash serve as both the integrity proof for the in-toto statement and the retention anchor for the audit log, eliminating the need for separate artifacts?
fail-20260917-Does-docs-build-policy-md-5-contain-a-cl	Does `docs/build-policy.md 5` contain a clause that automatically ratifies coverage claims in `compliance/SLSA.md` into enforceable bindings upon file publication?
fail-20260917-Does-the-hngh-automation-CI-pipeline-cod	Does the `hngh-automation` CI pipeline code contain any conditional logic that keys the execution or failure of the log-retention gate to a `slsa-level` metadata field?
fail-20260917-Does-the-hngh-kernel-s-existing-signing-	Does the `hngh` kernel's existing signing infrastructure support signing arbitrary JSON payloads (for the Composite Seal) or only binary digests, and what is the performance overhead of canonicalizing JSON for signing?
fail-20260917-How-does-the-Composite-Seal-pattern-inte	How does the "Composite Seal" pattern interact with `hngh-automation`'s current retention policiesspecifically, can a "hold marker" be added to a sealed segment without invalidating the original signature (e.g., via a sidecar or re-signing)
fail-20260917-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain any reusable workflow definitions that accept an `slsa-level` input parameter and conditionally execute log-pruning steps based on its value?
fail-20260917-Are-there-any-GitHub-Actions-job-outputs	Are there any GitHub Actions job outputs in `hngh-automation` named `slsa_level` or similar aliases that are consumed by subsequent jobs' `if:` conditions to control artifact retention?
patrol-20260917-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260917-Does-the-Sigstore-policy-engine-support-	Does the Sigstore policy engine support a "custom predicate handler" plugin that can interpret non-standard `predicateType` URIs without modifying the core validation logic?
fail-20260917-Is-there-a-precedent-in-the-W3C-PROV-O-o	Is there a precedent in the W3C PROV-O or other provenance standards for encoding data lifecycle metadata (e.g., TTL, legal hold) within the attestation envelope rather than as a sidecar?
fail-20260917-Does-the-Sigstore-project-have-any-open-	Does the Sigstore project have any open RFCs or design documents proposing a WASM-based or Go-plugin-based extension mechanism for custom predicate types in cosign?
fail-20260917-Is-there-a-userspace-component-in-the-hn	Is there a userspace component in the hngh repository, separate from the kernel OMP bridge, that implements a policy engine with a runtime plugin registration API for non-standard predicate URIs?
fail-20260917-Does-the-hngh-automation-kernel-tree-con	Does the `hngh-automation` kernel tree contain any existing "sidecar" or metadata index files (e.g., `.meta`, `.index`) that are already used for segment tracking, which could be repurposed for hold markers?
fail-20260917-Is-there-a-defined-audit-log-facility-in	Is there a defined "audit log" facility in `hngh-automation` that is itself sealed/signed, as required by R2/R4, or would this need to be built from scratch?
fail-20260917-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain a `RUNGS.md` file or any markdown/shell script that explicitly maps "R1" to specific verification commands?
fail-20260917-What-is-the-absolute-path-of-the-overnig	What is the absolute path of the overnight harness entry-point script (e.g., `run.sh`) in the `hngh-automation` checkout, and can it be cited as the canonical source for R1 verification?
fail-20260917-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain a `docs/security.md`, `spec/R2_R4.md`, or similar file that defines the exact cryptographic requirements for "sealed" and "signed" audit logs?
fail-20260917-Do-audit-records-in-hngh-automation-util	Do audit records in `hngh-automation` utilize the worker scratch store, and if so, does the path-collision bug (`obs-2026-08-26`) cause data loss or conflation of audit entries across wakes?
patrol-20260917-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260917-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260918-Does-the-path-collision-bug-in-the-scrat	Does the path-collision bug in the scratch store lead to incorrect computational values being recorded in audit logs, thereby compromising data integrity despite storage separation?
fail-20260918-How-does-the-current-log-rotation-policy	How does the current log rotation policy for `/var/log/hngh/audit/` handle concurrent writes from multiple workers during high-volume automation cycles, and are there any race conditions that could cause entry loss?
patrol-20260918-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-journal-error-unit-not-practiced	patrol: surface journal-error filed unit-not-practiced on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260918-github-ci-service-down	patrol: surface github-ci filed service-down on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260918-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain a `Makefile`, `Justfile`, or CI workflow definition that explicitly names the overnight entry-point command, and if so, what is its content?
fail-20260918-Is-R1-defined-in-the-hngh-automation-doc	Is "R1" defined in the `hngh-automation` documentation (e.g., `docs/` or `README.md`) as a specific test suite, artifact type, or gate condition, and if not, where is it defined in the broader project scope?
fail-20260918-How-does-the-current-cleanup-cycle-handl	How does the current cleanup cycle handle stale scratch directories if a worker-wake crashes mid-cleanup, and what state machine changes are needed to prevent reuse of these orphaned paths?
fail-20260918-What-integrity-constraints-e-g-checksums	What integrity constraints (e.g., checksums, append-only journals) are currently applied to audit log storage to prevent post-hoc tampering, independent of the scratch-store path collision issue?
fail-20260918-What-are-the-three-specific-candidate-fa	What are the three specific candidate failure classes for `gate-red`, and which one is currently tripping the automation-gate?
fail-20260918-Which-concrete-files-in-the-hngh-kernel-	Which concrete files in the `hngh` kernel repository define the precondition checks that cause `gate-red` to persist across consecutive runs?
patrol-20260919-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260919-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260919-Does-hngh-s-roguelike-run-model-single-e	Does `hngh`'s "roguelike run model" (single-execution lifecycle) inherently prevent durable state updates for unit practice, requiring an explicit persistence hook that is currently absent?
fail-20260919-Can-2026-08-30-alert-to-work-routing-pat	Can `2026-08-30-alert-to-work-routing-patterns-closing-the-self-observation-loop.md` be applied to identify the specific missing feedback loop in `hngh-automation` that fails to close the `unit-not-practiced` journal entry after remediation
patrol-20260919-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
synth-2026-09-19-1	Does the `fail-20260918-What-integrity-constraints-e-g-checksums` record's orthogonality framing correctly separate G0 inspection from audit-integrity constraints, and what specific checksum or hash verification steps are missing to make the audit trail verifiable?
synth-2026-09-19-2	How does the `patrol-20260915-journal-error-unit-not-practiced` state persistence defect interact with the `ledger-sync commit race` lesson, and can the hourly ledger-sync mechanism be modified to prevent machine-committed files from overwriting uncommitted lane-staged documentation?
fail-20260920-Does-the-hngh-kernel-s-current-store-lay	Does the hngh kernel's current store layer already compute per-entry SHA-256 digests, and if so, are they stored in a field adjacent to the record payload?
fail-20260920-What-is-the-exact-byte-level-serializati	What is the exact byte-level serialization format of audit trail entries in hngh (e.g., JSON key ordering, timestamp precision), and does it currently enforce UTC normalization at write time?
patrol-20260920-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260920-ctx-wiki-rebuild-project-eb53145f	why does the project vault rebuild fail/underperform and what fixes it?
patrol-20260920-gate-cure-gate-cure-refused	patrol: surface gate-cure filed gate-cure-refused on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260920-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
synth-2026-09-20-1	Does the `fail-20260920-What-is-the-exact-byte-level-serializati` record's structural decomposition of persistence failure modes align with the verified kernel ground-truth, and what specific byte-level serialization invariants must be enforced to close the loop?
synth-2026-09-20-2	How does the `patrol-20260915-journal-error-unit-not-practiced` finding's concrete state persistence defect manifest in the storage mechanism, and what auto-closure expectations are required to prevent consecutive failure escalation?
synth-2026-09-20-3	What specific code inspection steps are required to verify the two unverified load-bearing facts (staging scope and persistence failure mode) identified in the `synth-2026-09-19-2` record before implementation can proceed?
patrol-20260922-automation-gate-gate-red	patrol: surface automation-gate filed gate-red on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-pending-checks-check-pending	patrol: surface pending-checks filed check-pending on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-research-ledger-harvest-stale	patrol: surface research-ledger filed harvest-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260922-Does-the-research-lines-tsv-schema-curre	Does the `research-lines.tsv` schema currently include any fields for version pinning (e.g., commit hash, content hash) or timestamps that would allow deterministic stale detection?
fail-20260922-Is-the-outcome-demotion-at-two-consecuti	Is the `outcome-demotion-at-two-consecutive-failures` guardrail explicitly wired to count `harvest-stale` events as "bad executions," and if so, why did it not trigger closure after two consecutive runs?
fail-20260922-Does-the-hngh-automation-repository-cont	Does the `hngh-automation` repository contain a CI job or pre-commit hook that validates the schema of `research-lines.tsv` against a defined JSON Schema or CSV header template?
fail-20260922-Is-there-a-migration-log-or-changelog-en	Is there a migration log or changelog entry in the `hngh` repository that documents when version-pinning fields (e.g., `commit_hash`) were added to or removed from the `research-lines.tsv` schema?
fail-20260922-Does-the-hngh-kernel-repository-contain-	Does the `hngh` kernel repository contain any code or configuration that explicitly parses or validates the `research-lines.tsv` format, and if so, what error messages are generated for schema violations?
fail-20260922-Are-there-any-recent-commits-within-the-	Are there any recent commits (within the last 30 days) in `hngh-automation` that modify `research-lines.tsv` without corresponding changes to CI or pre-commit configurations, indicating ongoing reliance on manual discipline?
patrol-20260922-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-journal-error-unit-not-practiced	patrol: surface journal-error filed unit-not-practiced on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260922-pending-checks-check-pending-stale	patrol: surface pending-checks filed check-pending-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260922-What-is-the-exact-staleness-predicate-in	What is the exact staleness predicate in the untruncated Anomalies.scala  threshold value, timestamp source, and reset condition  and does it resolve hypothesis (a) vs (b)?
fail-20260922-Is-outcome-demotion-at-two-consecutive-f	Is outcome-demotion-at-two-consecutive-failures implemented anywhere in the hngh kernel or scripts/, and if not, what is the minimal resolver that closes the check-pending-stale lifecycle?
synth-2026-09-23-1	Does the `patrol-20260922-research-ledger-harvest-stale` guardrail wiring require a new state transition in the ledger schema to close the loop on the steer-vs-die threshold?
synth-2026-09-23-2	Can the `fail-20260922-Does-the-research-lines-tsv-schema-curre` header row inspection be automated into a pre-commit hook to enforce TSV integrity before the next harvest?
synth-2026-09-23-3	What is the exact staleness predicate in `fail-20260922-What-is-the-exact-staleness-predicate-in` that distinguishes a stale research line from a parked one under the current kernel ground-truth pass?
patrol-20260923-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-pending-checks-check-pending-stale	patrol: surface pending-checks filed check-pending-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-rotation-due-rotation-due	patrol: surface rotation-due filed rotation-due on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-journal-error-unit-not-practiced	patrol: surface journal-error filed unit-not-practiced on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-gate-cure-gate-cure-refused	patrol: surface gate-cure filed gate-cure-refused on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260923-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
synth-2026-09-23-1	What is the exact staleness-predicate-in the patrol-20260922-pending-checks-check-pending-stale findings?
synth-2026-09-23-2	How can the "steer-vs-die threshold" be codified into a missing-design class in the bestiary.md?
synth-2026-09-24-1	patrol-20260922-research-ledger-harvest-stale — what is the exact guardrail wiring and ledger schema that must be verified to close the steer-vs-die threshold loop identified in the recurring failure pattern?
synth-2026-09-24-2	patrol-20260922-pending-checks-check-pending-stale — what are the exact demotion-vs-dedup and same-check-vs-distinct-check distinctions that must be resolved before any guardrail is implemented?
synth-2026-09-24-3	patrol-20260923-rotation-due-rotation-due — what is the evidence for the broken dedupe key and demotion reachability assumptions that currently block OT1 and OT2 progress?
patrol-20260924-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260924-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260924-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260924-pending-checks-check-pending-stale	patrol: surface pending-checks filed check-pending-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260924-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260924-Does-the-slug-line-ID-minting-code-that-	Does the slug/line-ID minting code that produced `patrol-20260923-rotation-due-rotation-due` share a key-composition code path with the outcome dedupe-key derivation, making the doubled token a reproduction of the suspected bug?
fail-20260924-What-are-OT1-and-OT2-concretely-tracker-	What are OT1 and OT2 concretely (tracker entries, owners, acceptance criteria), and is the blocking mechanism linking them to the dedupe/demotion hypotheses written down anywhere verifiable?
fail-20260924-Do-any-of-the-five-related-findings-docu	Do any of the five related findings documents (clean-architecture-roguelike-run-review, adversarial-review-patterns, gantt-legibility, session-cost-display, alert-to-work-routing) contain hidden references to OT1/OT2, blocking mechanisms, o
fail-20260924-Can-the-four-prior-art-sources-stall-les	Can the four prior-art sources (stall-lessons, mid-line-verification, outcome-demotion, supervision-native) be located and re-read to independently verify the reconstructed blocking chain before accepting F2 as final?
patrol-20260924-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260924-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
synth-2026-09-25-1	Execute the dedupe-key probe from synth-2026-09-24-3 (the adopted line) by tracing the slug-line-ID minting code path and verifying whether the doubled `rotation-due` token is a live reproduction of the suspected dedupe-key bug rather than a crystallized hypothesis.
fail-20260924-crumbs-mirror-rows	Which writer appended the duplicated crumbs-mirror rows into STATE.md on 2026-09-24, and what parity deficit remains between mirrored and source rows before the crumbs reader flip can clear its 2026-10-01 SLA?
patrol-20260925-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-pending-checks-check-pending-stale	patrol: surface pending-checks filed check-pending-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-systemd-units-timer-dead	patrol: surface systemd-units filed timer-dead on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on consecutive runs -- why does the check still find no gate crumb against the crumbs-db seam after the refoundation moved crumb readers off STATE.md, and which guardrail or re-baseline closes it?
synth-2026-09-25-2	Do any of the five related findings documents document the blocking mechanism for the "suspected dedupe-key bug" reproduction in `fail-20260924-Does-the-slug-line-ID-minting-code-that-`?
synth-2026-09-25-3	What is the exact staleness predicate in `fail-20260922-What-is-the-exact-staleness-predicate-in` that the line's own verifiability standard demands production on?
patrol-20260925-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260925-journal-error-unit-not-practiced	patrol: surface journal-error filed unit-not-practiced on two consecutive runs -- why does it keep failing and which guardrail closes it?
fail-20260925-supervision	What knowledge was missing for supervision?
patrol-20260925-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-automation-gate-gate-stale	patrol: surface automation-gate filed gate-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-feeds-feed-stale	patrol: surface feeds filed feed-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-github-ci-bad-execution	patrol: surface github-ci filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-handoffs-bad-execution	patrol: surface handoffs filed bad-execution on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-manga-manga-stale	patrol: surface manga filed manga-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-pending-checks-check-pending-stale	patrol: surface pending-checks filed check-pending-stale on two consecutive runs -- why does it keep failing and which guardrail closes it?
patrol-20260926-journal-error-unclaimed-err	patrol: surface journal-error filed unclaimed-err on two consecutive runs -- why does it keep failing and which guardrail closes it?
arc-20260925-descent-adoption-gate	descent: what enforces crystallized-line adoption-or-revoke with a named consumer and one-alternation deadline (design/descent.md), and what fixture proves it?
arc-20260925-descent-audit-station	descent: what weekly beat shape runs the five falsifiable checks and the write-only artifact count (design/descent.md), and where does its row land?
arc-20260925-dream-graph	dream: how do dream briefs cite completion-graph nodes so proposals stay grounded and budgeted under P6/P7?
arc-20260925-local-vllm-probe	local-models: what do a uv venv + python-vllm-rocm smoke on RX 7900 XT measure for billion-context compression, and under what VRAM/coordination halts?
arc-20260925-os-adapters	os: what real probe capability turns the static harness adapters executable while execute() stays allowlisted fail-closed?
arc-20260925-os-package-pairing	os: what daily check pairs hngh-packages.tsv pins with pacman reality so drift files a subject, never an auto-update?
arc-20260925-debt-ledger	debt: where do the ~19 ponytail ceiling markers index into a ledger an audit beat can consume?
question-synth-2026-09-26-1	How does the descent-audit-station's 0.99-confidence parked verdict resolve when the crystallization contains zero findings and no verified paths, and what ground-truth evidence is required to either confirm or reject the parking decision?
question-synth-2026-09-26-2	What is the operational impact of the tree-skew whitelist hiding the plan ledger from the monitor, and how can the plan ledger be versioned and made visible to the oversight-tick without reintroducing tree-skew flapping?
synth-2026-09-26-3	What rule should govern steer-vs-kill on stalled sessions when no decided threshold exists and judgment is currently ad hoc per session, and how can this be formalized to prevent the loop-recognition pattern of ten tree-skew candidates on
arc-20260926-descent-adoption-gate	wire the descent adoption gate per docs/design/descent.md:129-147 -- crystallized line -> backlog row with named consumer + one-alternation deadline, expired adoptions auto-revoke to a citable dead-letter (prior arc-20260925 line died in review for an empty crystallization, not a rejected thesis)
