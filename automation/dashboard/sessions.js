/* sessions.js — retired. The session observatory moved into the unified
   dashboard tab (index.html#tab-sessions, rendered by sessions-view.js).
   sessions.html redirects there; this stub only keeps any stale references
   from 404ing. The sessions.json feed (jobs/sessions-feed.py) is unchanged
   as an endpoint — it just carries parsed entries now. */
