# MC-2 wave 1 — hngh-mc.el (Emacs mission-control dashboard)

Session: 2026-07-31 · moonshotai/kimi-k3 (Hermes TUI) · worker: opencode (opencode/kimi-k3)
Plan: ~/Projects/etc/sysconfig_mgmt/.omc/plans/mission-control-v2.md (wave MC-2)

## GUARDRAILS (non-negotiable)

- Touch ONLY the two files named below. No other file edits, no git commits.
- Never run `git add -A` / `git commit`. Leave the tree dirty; the orchestrator commits.
- Plain elisp, lexical-binding, `;;;###autoload` where useful. No new packages:
  zoom, eat, comint are available; use built-ins otherwise.
- Keep code free of comments that narrate; docstrings only.
- Verify with batch byte-compile (see Verification). Do not open GUI frames.

## Environment facts (verified — trust these)

- Emacs 30.2. straight.el + use-package in use; zoom installed and bound
  (`C-c z`, `zoom-size '(0.75 . 0.8)`). eat IS available (straight repo `eat/`).
- `hngh-status` binary on PATH: prints process/queue/events sections as text.
- Event journal dir: `~/.hngh/journal/events/` — files `YYYY-MM-DD.lisp`, one
  sexp per line-ish (lisp data, tail-friendly).
- llmtrim: `llmtrim status` prints the savings dashboard (unicode box chars).
- svc-dash: run `uv run --project ~/Projects/etc/svc-dash python -m svc_dash.app`
  (a Textual TUI — embed via eat terminal, NOT comint).
- mc: `~/.local/bin/mc` manages the tmux fallback session (`mc status`).
- hngh daemon lifecycle: `make run` in ~/Projects/etc/hngh (SBCL). For wave 1
  the daemon is managed by tmux (`mc`), NOT by this elisp — read-only panels only.

## TASK 1 — ~/Projects/etc/hngh/emacs/hngh-mc.el (new file; create dir)

Provide:

1. `hngh-mc-open` (interactive): pops a frame-local dashboard layout —
   left window `*hngh-status*` (run `hngh-status`, `special-mode`-derived,
   `revert-buffer` re-runs the command; auto-revert every 5s via
   `hngh-mc--refresh-timer`), right-top `*hngh-events*` (tail of newest file in
   `~/.hngh/journal/events/`, `auto-revert-tail-mode`, and when a newer
   YYYY-MM-DD.lisp appears switch to it), right-bottom `*llmtrim*` (output of
   `llmtrim status`, refreshed on a 30s timer; strip nothing, box chars fine).
2. `hngh-mc-refresh` (interactive): refresh all panels now.
3. `hngh-mc-svc-dash` (interactive): open `*svc-dash*` in an `eat` terminal
   running the uv command above; if eat fails to load, fall back to a
   `compilation-mode` buffer and say so in the mode line.
4. `hngh-mc-tmux-status` (interactive): message-line summary of `mc status`.
5. Clean timer teardown: `hngh-mc-close` kills timers + dashboard buffers.
6. zoom-mode is USER-managed (C-c z). Do not enable or reconfigure it; the
   layout must simply tolerate resizing (no fixed window sizes; use
   `display-buffer` with sensible alist defaults).

Byte-compile clean: zero warnings under Emacs 30.2 batch byte-compile with
`byte-compile-error-on-warn` nil — report warnings honestly.

## TASK 2 — ~/Projects/etc/hngh/emacs/README.md (new file)

Short: what it is, load instructions
`(load "~/Projects/etc/hngh/emacs/hngh-mc.el")`, keybindings/commands table,
note that tmux `mc` remains the headless fallback and manages the daemon.

## VERIFICATION (must run, paste real output in final message)

1. `emacs -Q --batch -L ~/.emacs.d/straight/repos/eat \
   --eval '(setq byte-compile-error-on-warn nil)' \
   -f batch-byte-compile ~/Projects/etc/hngh/emacs/hngh-mc.el` — exit 0.
2. `emacs -Q --batch --load hngh-mc.el --eval '(progn (hngh-mc-open) (hngh-mc-refresh) (message "MC2_OK"))' 2>&1 | tail -5` — shows MC2_OK, no backtrace.
3. List every file you created/modified with `git -C ~/Projects/etc/hngh status --short`.

Report: files changed, compile warnings (verbatim), any deviation from this
spec and why.
