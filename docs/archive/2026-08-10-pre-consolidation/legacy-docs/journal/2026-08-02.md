# 2026-08-02

## M7 client-server daemon — merged to main (`28d92ad`, `f6e69dc`)

- M7 wire protocol handlers + daemon core + client CLI landed; systemd units
  reconciled after the a3-merge (daemon wins) — `f6e69dc`.
- Client socket closure during daemon shutdown handled — `40785b3`.
- ROLE-ACK lookup decision amended into the Phase 2 spec (`106a922`).

## Phase 2 — atomic claim/release + lease expiry

- `1b71044` claim/release GREEN transitions; `e596d8c` RED fixtures + guards;
  `cf11d75` task transition RED fixtures.
- `199b112` Phase 2 completion transitions; `a1809ad` PM-failure authority
  coverage.
- `41b5506` scheduler expires claimed leases; `cdde12c` lease-expiry RED
  fixtures.
- `1f645f2` M7 wire handlers for Phase 2 operations.
- `2ca9a91` post-Phase-2 count 1120/1120.
- Test-count corrections: 1028/1028 @ 106a922, 1120/1120 @ 1f645f2.

## Task 83 + mission-control hardening

- `d569e3b`/`6150eeb` session-tree unit tests + cascade-restart fix.
- `e5b531c` pane state persistence / layout restore.
- `f081443` config-watcher + targeted reload.
- `c3055f0` dashboard TUI QoL (help panel, floor indicators).

## Squads — lifecycle + platoons

- `344fc4c` local squad lifecycle; `c9eb9f5` local squad registry.
- `d136b7c` / `a96050c` / `e06182f` queue-runner, base-role, review-role
  prompts/specs; `d1c4d37` duo review spec.
- Agent platoons design (`de08375`) + coordination record (`4cc90da`).
- Preserved squad run journals + session wave artifacts (`f9af4cc`,
  `19ff9b5`).

## Aesthetics/brand

- Blame!-inspired TUI header + brand spec (`91ccb8a`), aesthetic identity
  doc (`4606060`), brand + TUI aesthetics from Artist (`3e74d06`), daemon
  splash design (`d3b07c0`), megastructure manifesto (`56aead0`), TUI mockups
  (MC overview, task queue, squad view — `2d3f290`), Phase 3 megastructure
  TUI plan (`cb438c7`).

## What's next

- M9 squad autonomy W1-3 (agents-md, resource gate, fragment journal) next
  day; C7 PM-first-prompt generator.

## Decisions

- Mini-ADR: M7 daemon wins over the a3 branch for systemd ownership
  (`f6e69dc`) — single source of truth for unit state.
- Mini-ADR: ROLE-ACK — ambiguity resolved by lookup decision in the Phase 2
  spec (`106a922`).