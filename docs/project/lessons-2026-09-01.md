# Lessons — 2026-09-01

## Overnight continuity wave (2026-09-01 plan, executed 2026-09-01)

Wrap notes from the plan's step 5, appended by the 02:00Z wake
(steps 1–3 landed by the 01:00Z wake, 4–5 by the 02:00Z wake):

- The routing loop's observation edge is closed end-to-end and ran
  unattended before the wrap: the hourly router-feed caller
  (hngh-automation 7992f78) fed 3 identities at 02:00:45Z and behaved
  correctly on all three — duplicate-skip on a closed-step re-fire
  (gate-check:plan:2026-08-30-overnight-continuity:step-1) and
  already-routed skips on both routed alerts (STATE.md rows). The
  first real alerts became plan candidates (reports.md bffc89a6,
  ffa1d58e) and the accept-plans gate auto-accepted both (f4c7e12e,
  9993c29d). No human hand anywhere in the chain except parked
  classes.
- Pre-flight refusal caught a real gate violation before a ceremony
  burned: two new reports.md session-touch alert rows carried an
  absolute home path and verify-candidate refused. Normalized to
  ~/-form for the landing; the upstream writer still emits absolute
  paths, so every future session-touch row re-risks the same refusal
  — price a report-queue-side normalization (emit ~/-form) before the
  next reports.md ceremony.
- The master-plan §4 research backlog is fully crystallized: honest
  gamification mechanics (docs/research/
  2026-09-01-honest-gamification-mechanics.md, 16/16 grounding paths
  re-verified live at ceremony time) was the last candidate. Next
  queued research candidate: ebook-book-inputs.
- Two certificate ceremonies in one wake beat (~38s each) with both
  gates re-run in-beat — the beat-sizing law holds: gates 36s,
  ceremonies under 80s total, the rest is authoring.
- Unread alert count is still monotonic (544 at the 02:00Z wake). The
  router now drains routable classes into plan candidates; the growth
  that remains is operator-owned classes (stale-store, session-touch)
  plus the unread backlog itself — whether routed-stub wakes actually
  bend the unread trend is the next honest observation, not a claim.
