<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-07 — next plan: identifier lint, model-bench live run, external research verification

Authorization: operator-directed 2026-09-07, recorded faithfully in
docs/records/2026-09-01-operator-items-plan.md (parked follow-on candidates)
and docs/BACKLOG.md (identifier-consistency lint, external resources).

Grounding notes (evidence checked 2026-09-07 ~02:00Z):
- Operator-items plan fully executed (all 9 steps); parked items: model-bench
  live run, research-lines wiring, SMTP/ntfy credentials, kernel DelegationQueue.
- Kernel gate plan parked: test_dry_run_exits_zero_and_mutates_nothing fails
  (cadence docs contain "heartbeat" text; negative-assertion invariant violated).
  FORBIDDEN this session — park with alert row.
- Backlog items: identifier-consistency lint (observed 2026-08-25), external
  resources (Chart Library, AI Agents Archive, Ainglish, Dynamic Feed).
- No existing next-plan file exists (no 2026-09-07 plan file on disk).

Autonomy rule (standing): hngh docs changes land via certificate ceremony with
green `make test`; hngh kernel src/, tests/, Makefile, hngh.asd changes are
FORBIDDEN to machine sessions — park them with an alert row instead.
hngh-automation script work lands as plain commits gated by hngh-automation
`make test`. Never touch provider or credential configuration, systemd unit
lifecycle beyond an already-installed unit, tracked deletions outside the
48h prune, or secrets.

Paced-cadence contract: beats bounded at ≤ ~60m wall each; strict grow↔research
alternation per master-plan §4; every step names its own verification and is
executable by a bounded delegated session with no human present.

## Steps

- [ ] 1. RESEARCH — identifier-consistency lint design. Design
      `scripts/lint-identifiers.sh` per the backlog row: (a) extracts every
      `$UPPER_NAME` reference and `NAME=` definition from `config.env`,
      `lib/*.sh`, `jobs/*.sh`, `scripts/*.sh`; (b) flags referenced-but-never-
      defined and defined-but-never-referenced names; (c) checks every
      canonical name (UNSLOTH_URL, OLLAMA_URL, MODEL, …) against an exact-
      spelling dictionary so near-miss spellings are named explicitly.
      Document the design in docs/research/2026-09-07-identifier-lint-design.md;
      the actual implementation is a separate grow beat.
      Verification: the design doc exists with named canonical names, the
      three-pass structure, and a "how we know it works" section citing the
      `UNSLOOTH_FALLBACK_MODELS` deliberate reintroduction test; kernel
      `make test` green.

- [ ] 2. GROW — model-bench live run (first beat). Execute the model-bench
      script (jobs/model-bench.sh) and record the output to
      stats/model-bench-2026-09-07.jsonl. The script appends one JSON line per
      model (three deterministic probes, python judge, reloads the primary
      model warm at end). Report the results: pass counts, tokens/s, any
      failures. This is a grow beat (executing the existing tool).
      Verification: stats/model-bench-2026-09-07.jsonl exists with ≥3 model
      rows; the file is committed to hngh-automation with a plain commit;
      `git status -sb` shows the branch up to date with origin (push-on-demand
      verified).

- [ ] 3. RESEARCH — external resources read-only verification. For each of
      the four backlog external resources (Chart Library, AI Agents Archive,
      Ainglish, Dynamic Feed), perform read-only verification: confirm the
      URL is reachable, the documented endpoints exist (e.g. /api/v1/...,
      /openapi.json), and the keyless claims hold. Document findings in
      docs/research/2026-09-07-external-resources-verification.md with a
      per-resource table: URL, status (reachable/unreachable), key endpoints
      verified, remaining blockers. Ground claims in observed responses;
      "not established" where evidence is thin.
      Verification: the research doc exists with a per-resource table and
      explicit "not established" markers where unverified; kernel `make test`
      green.

- [ ] 4. GROW — operator-items follow-on: model-bench cost comparison. Compare
      the model-bench results (step 2) against session-cost telemetry from
      jobs/session-cost.py (stats/model-bench-*.jsonl + logs/budget.md). Quantify
      the current per-session cost distribution; note any gaps. This is a
      grow beat (reading existing data, writing analysis).
      Verification: the comparison is recorded in a research doc (or the
      operator-items plan file, updated) with concrete telemetry rows cited;
      kernel `make test` green.

- [ ] 5. RESEARCH — SMTP/ntfy credential configuration analysis. The operator-
      items plan parked SMTP/ntfy credential and provider configuration as
      critical-class work. Design the minimal configuration surface: what
      provider (SMTP/ntfy/Apprise), what credentials (URL, key, from-address),
      where they live (config.env? lib/credentials.sh?), and how they get
      wired into the notify-email slice. Document the design in
      docs/research/2026-09-07-notify-email-config-design.md. This is research
      (docs only, no code changes to provider/credential configuration).
      Verification: the design doc exists with named provider options, credential
      fields, and a "not established" framing where the operator must decide;
      kernel `make test` green.

- [ ] 6. GROW — wrap, lessons, author-next-next-plan. Land this plan's lessons
      into the foldback/lessons path (docs/records/2026-09-07-lessons.md), and
      author the next-next plan file (docs/project/plans/2026-09-07-next.md,
      status=proposed) whose steps cover the newest executed evidence and the
      remaining parked items (kernel DelegationQueue — forbidden; research-lines
      wiring — backlog row; operator decision points).
      Verification: the lessons file exists with named findings; the next-next
      plan file exists with contract-valid front-matter (first line
      `<!-- plan: status=proposed risk=normal accepted=- -->`) and every unchecked
      step carrying an indented Verification line; kernel `make test` green.

## Parked (not in this plan, recorded for the operator; follow-on candidates
for the next-next plan's author):
- Kernel-side DelegationQueue internals (hngh src/tests/Makefile/hngh.asd) —
  forbidden by the autonomy rule; parks with alert rows.
- research-lines wiring decision for generate-publication (backlog row, queued).
- Operator decision on SMTP/ntfy provider selection (step 5 designs the
  configuration surface; the operator chooses).
- Operator decision on external resources participation (Ainglish seconds/
  measurements are public and permanent).

## Autonomy rule (binding for this session)

Governance — certificates and green gates — is the only barrier. Do not
wait for or ask for human approval. hngh-automation commits are free.
hngh changes land via the certificate ceremony ONLY with a green `make test`.
hngh kernel src/, tests/, Makefile, and hngh.asd changes are FORBIDDEN this
session — if a step requires one, stop that step, note it in the plan file,
and move to the next step. Never touch provider or credential configuration,
systemd unit state, tracked deletions outside the 48h prune, or secrets.
If blocked, write what blocked you into the plan file and move on; other work
always exists.

## Standing authorizations (operator grant, 2026-09-01)

Push origin whenever a commit exists — push-on-demand is authorized;
NEVER pause for or ask push confirmation (on push failure: file an alert row
and continue). Never pause to ask the operator anything — a question is
either answerable from repo docs or becomes a parked alert row. The email
digest may run in report mode anytime.
