<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Create `scripts/validate_plan.sh` to check for the presence of required plan files in the `jobs/` directory.
  Verification: bash -n scripts/validate_plan.sh
- [ ] Add a test case in `tests/test_validate_plan.sh` that asserts the validation script exits with code 0 when valid plans are present.
  Verification: make test
- [ ] Update `lib/plan_utils.py` to include a function that parses plan metadata from YAML files without external dependencies.
  Verification: python3 -c "import sys; sys.path.insert(0, 'lib'); import plan_utils"
- [ ] Create `cadence/schedule_check.sh` to verify that cadence definitions reference existing job IDs.
  Verification: bash -n cadence/schedule_check.sh
