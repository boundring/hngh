<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

Implements the cadence-interval-drift research line by bounding retry backoff in job dispatch and surfacing accumulated drift in the periodic digest.

## Steps

- [ ] Add lib/cadence_backoff.py exposing compute_delay(attempt, base=2, cap=60) and tests/test_cadence_backoff.py with stdlib-only assertions
  Verification: make test
- [ ] Wire compute_delay into scripts/dispatch_job.sh retry loop replacing the fixed-sleep branch
  Verification: bash -n scripts/dispatch_job.sh
- [ ] Extend digest/build_digest.py to append a cadence-drift section sourced from lib/cadence_backoff.py
  Verification: python3 digest/build_digest.py
- [ ] Add dashboard/cadence_status.js rendering the drift snapshot as plain text
  Verification: node --check dashboard/cadence_status.js
- [ ] Add jobs/cadence_probe.sh that invokes scripts/dispatch_job.sh in dry-run mode for smoke checks
  Verification: bash -n jobs/cadence_probe.sh
