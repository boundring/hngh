<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the research line for standardizing job execution wrappers by introducing a reusable shell library to handle common setup and teardown tasks within the `jobs/` directory, ensuring consistent environment configuration without altering core kernel or security boundaries.

## Steps

- [ ] Create `lib/job_helpers.sh` containing functions for setting up temporary directories and logging initialization
  Verification: bash -n lib/job_helpers.sh

- [ ] Add a unit test script `tests/test_job_helpers.sh` that sources the library and asserts the existence of created temp directories
  Verification: python3 tests/test_job_helpers.sh

- [ ] Update an existing job in `jobs/` to source `lib/job_helpers.sh` instead of using inline setup logic
  Verification: make test

- [ ] Add a static analysis check script `scripts/check_shell_syntax.sh` that iterates through all `.sh` files in `jobs/` and runs `bash -n` on them
  Verification: bash scripts/check_shell_syntax.sh
