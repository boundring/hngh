<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the cadence-idempotency research line, which makes recurring hngh jobs rerun-safe and observable via a stable run key folded into a daily digest. All steps land as small, test-gated commits in hngh-automation.

## Steps

- [ ] Add `lib/cadence_key.py` (stdlib-only) exposing `run_key(job, slot, payload)` that returns a stable SHA-256 string, plus `tests/test_cadence_key.py` asserting identical inputs yield identical keys.
  Verification: python3 tests/test_cadence_key.py
- [ ] Update `jobs/cadence_sync.sh` to compute and log the run key via `lib/cadence_key.py` before processing, making reruns idempotent.
  Verification: bash -n jobs/cadence_sync.sh
- [ ] Add `digest/summarize.py` (stdlib-only) that folds per-slot run keys into one digest line each, plus `tests/test_digest_summarize.py`.
  Verification: python3 tests/test_digest_summarize.py
- [ ] Add `dashboard/render.js` (no external deps) that prints the digest lines as a plain-text table to stdout.
  Verification: node --check dashboard/render.js
- [ ] Add `scripts/validate_cadence.sh` that greps `cadence/` for duplicate slot names and exits non-zero
