<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-16 - dev-fail-20260915-Does-the-obs-2026-08-25-hn (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the research line on duplicated-and-diverged dashboard behaviors by adding a contract test that pins shared rendering logic to prevent silent divergence.

## Steps

- [ ] Create `tests/dashboard/test_shared_behavior_contract.py` with a stdlib-only test asserting that two nominally-shared dashboard modules expose identical function signatures for their core rendering entry points.
  Verification: python3 tests/dashboard/test_shared_behavior_contract.py
- [ ] Add a regression case in `tests/dashboard/test_shared_behavior_contract.py` that imports both candidate implementations and asserts their output for a fixed input fixture is byte-identical.
  Verification: python3 tests/dashboard/test_shared_behavior_contract.py
- [ ] Create `lib/dashboard/shared_behavior_registry.json` listing the canonical module paths and function names that constitute the shared contract surface.
  Verification: grep -q "shared_behavior_registry" lib/dashboard/shared_behavior_registry.json
- [ ] Add a guard in `tests/dashboard/test_shared_behavior_contract.py` that fails if any module listed in the registry is missing or does not expose the declared entry points.
  Verification: python3 tests/dashboard/test_shared_behavior_contract.py
- [ ] Run the full hngh-automation test suite to confirm the new contract tests pass and no existing behavior regresses.
  Verification: make test
