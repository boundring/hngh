<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-16 - dev-fail-20260915-Does-the-obs-2026-08-25-hn (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the research line on duplicated-and-diverged dashboard behaviors by adding a contract test that pins shared rendering logic against drift.

## Steps

- [ ] Add a shared helper module under lib/ that centralizes the tab-state persistence key derivation used by the dashboard.
  Verification: make test

- [ ] Update the dashboard source to import and call the new shared helper instead of its local copy.
  Verification: grep -q "require.*lib" dashboard/index.js

- [ ] Add a unit test under tests/ that asserts the shared helper returns the expected persistence key for a fixed session ID.
  Verification: make test

- [ ] Add a regression test under tests/ that loads the dashboard module and verifies it delegates to the shared helper rather than duplicating logic.
  Verification: make test
