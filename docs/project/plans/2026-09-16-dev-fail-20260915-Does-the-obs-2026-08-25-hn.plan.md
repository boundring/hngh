<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-16 - dev-fail-20260915-Does-the-obs-2026-08-25-hn (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the research line on duplicated-and-diverged dashboard behaviors by adding a contract test that pins the shared scroll behavior to prevent silent divergence across copies.

## Steps

- [ ] Create a new shell script under `tests/` that asserts the shared scroll behavior constant is defined identically in both candidate implementation files
  Verification: bash -n tests/test_scroll_contract.sh
- [ ] Add a grep assertion inside the test script that confirms the scroll behavior string appears in at least two distinct source files without divergence markers
  Verification: grep -q "SCROLL_BEHAVIOR" tests/test_scroll_contract.sh
- [ ] Update the `make test` target or existing test harness to invoke the new contract script as part of the standard suite
  Verification: make test
- [ ] Commit the new test file and any necessary Makefile adjustments as a single plain commit in hngh-automation
  Verification: git log --oneline -1
