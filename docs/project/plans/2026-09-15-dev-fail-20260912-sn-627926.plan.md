<!-- plan: status=accepted risk=normal accepted=2026-09-18T01:41:57Z -->
# 2026-09-15 - dev-fail-20260912-sn-627926 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the fail-20260914-Does-the-github-ci-workflow-definition-c research line by adding a mechanical drift check for the patrol verdict rule between the CI workflow surface and the kernel canonical source, preventing independent edits from diverging.

## Steps

- [ ] Create `lib/verdict-rule-source.sh` that reads the canonical patrol verdict rule from the kernel path and emits it to stdout
  Verification: bash -n lib/verdict-rule-source.sh
- [ ] Create `scripts/check-verdict-drift.sh` that invokes the source script and greps the github-ci workflow definition for an embedded copy, exiting non-zero on mismatch
  Verification: bash -n scripts/check-verdict-drift.sh
- [ ] Add a test fixture under `tests/fixtures/verdict-rule-canonical.txt` containing the expected canonical rule text
  Verification: grep -q "patrol" tests/fixtures/verdict-rule-canonical.txt
- [ ] Wire the drift check into the existing make test target by adding a line in the Makefile that runs scripts/check-verdict-drift.sh
  Verification: make test
- [ ] Add a regression test under `tests/test_verdict_drift.sh` that asserts the check script fails when the workflow copy is altered
  Verification: bash tests/test_verdict_drift.sh
