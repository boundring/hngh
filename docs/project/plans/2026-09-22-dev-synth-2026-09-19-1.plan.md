<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a shell script `scripts/verify-utc-normalization.sh` that greps for the fixed-width UTC timestamp pattern in `src/main.lisp` and asserts the presence of `format-utc-timestamp`.
  Verification: bash -n scripts/verify-utc-normalization.sh

- [ ] Create a test file `tests/test-utc-invariant.sh` that sources the verification script and exits non-zero if the UTC normalization invariant is not found in the source.
  Verification: bash tests/test-utc-invariant.sh

- [ ] Update `Makefile` to include a `test-utc` target that executes `bash tests/test-utc-invariant.sh`.
  Verification: make test

- [ ] Add a comment block in `lib/serialization-notes.md` documenting the byte-level serialization invariants confirmed by the ground-truth pass.
  Verification: grep -q "UTC normalization" lib/serialization-notes.md
