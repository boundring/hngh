<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the "Standardized Job Lifecycle" research line by establishing a uniform execution contract for all hngh-automation jobs, ensuring consistent logging and exit code handling without altering core kernel or security boundaries.

## Steps

- [ ] Create `lib/job_contract.sh` defining standard `job_start` and `job_end` functions that handle timestamping and exit code propagation
  Verification: bash -n lib/job_contract.sh

- [ ] Update `jobs/run_pipeline.sh` to source `lib/job_contract.sh` and wrap the main execution block with `job_start` and `job_end` calls
  Verification: make test

- [ ] Add `tests/test_job_contract.sh` containing a mock job that asserts `job_end` correctly captures non-zero exit codes from simulated failures
  Verification: bash tests/test_job_contract.sh

- [ ] Create `cadence/health_check.sh` that executes a lightweight internal consistency check using only standard library tools to verify repository integrity
  Verification: python3 cadence/health_check.py

- [ ] Update `dashboard/status_report.sh` to parse the latest job log output and format a human-readable summary of pass/fail states
  Verification: bash -n dashboard/status_report.sh
