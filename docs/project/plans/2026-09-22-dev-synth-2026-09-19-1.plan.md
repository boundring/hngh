<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the research line for enhancing hngh-automation's CI reliability by adding a pre-flight syntax validation gate that checks all bash and Python scripts before execution, ensuring early failure detection without altering core kernel or provider logic.

## Steps

- [ ] Create `scripts/precheck_syntax.sh` to iterate through `jobs/`, `lib/`, and `tests/` directories and run `bash -n` on `.sh` files and `python3 -m py_compile` on `.py` files
  Verification: bash -n scripts/precheck_syntax.sh

- [ ] Add a `make precheck` target to the root Makefile that invokes `scripts/precheck_syntax.sh` before the standard test suite
  Verification: make precheck

- [ ] Update `cadence/ci_pipeline.sh` to call `make precheck` as the first step in the pipeline execution sequence
  Verification: grep -q "make precheck" cadence/ci_pipeline.sh

- [ ] Add a unit test in `tests/test_precheck_syntax.sh` that creates a temporary invalid bash file and asserts the precheck script fails with non-zero exit code
  Verification: bash tests/test_precheck_syntax.sh

- [ ] Run the full test suite to ensure the new precheck integration does not break existing job definitions or library functions
  Verification: make test
