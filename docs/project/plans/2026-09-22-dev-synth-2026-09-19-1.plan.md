<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the ground-truth findings from `synth-2026-09-20-1` by adding a static verification script to the automation layer that asserts the UTC timestamp normalization invariants (fixed-width `YYYY-MM-DDTHH:MM:SSZ` format) and confirms the presence of the corresponding kernel test gates, ensuring the serialization boundary remains consistent without modifying kernel source.

## Steps

- [ ] Create `scripts/verify-timestamp-invariant.sh` that uses grep to assert the existence of the `format-utc-timestamp` function definition in `src/main.lisp` and the fixed-width format string pattern.
  Verification: bash -n scripts/verify-timestamp-invariant.sh

- [ ] Add a check to `scripts/verify-timestamp-invariant.sh` that greps for the specific UTC format regex or constant within `src/domain/attestation.lisp` to confirm the shape pinning is present.
  Verification: bash scripts/verify-timestamp-invariant.sh

- [ ] Create `tests/test_timestamp_invariant.py` (stdlib only) that imports a mock record structure and asserts that any timestamp string matching the invariant regex contains exactly one 'Z' suffix and no offset indicators.
  Verification: python3 tests/test_timestamp_invariant.py

- [ ] Update `cadence/automation.sh` to invoke `scripts/verify-timestamp-invariant.sh` as a pre-flight check before running the kernel test suite, ensuring the automation layer validates source invariants.
  Verification: grep -q "verify-timestamp-invariant.sh" cadence/automation.sh

- [ ] Add a comment block to `lib/serialization.py` (or equivalent lib file) documenting the byte-level serialization invariant derived from `synth-2026-09-20-1`, explicitly stating that records are s-expression lines, not JSON.
  Verification: grep -q "s-expression" lib/serialization.py
