<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

Implements the research line on cadence-aware pre-flight validation for job scheduling, adding a slot-computation helper, a shell preflight gate, and their test coverage as plain additive commits under hngh-automation's existing make-test gate.

## Steps

- [ ] Add lib/cadence.py exposing next_slot(cadence_expr) and a __main__ self-check that asserts a known expression resolves
  Verification: python3 lib/cadence.py
- [ ] Add scripts/preflight.sh that exits non-zero if any .sh file under jobs/ fails bash -n, with a usage guard for direct invocation
  Verification: bash -n scripts/preflight.sh
- [ ] Add jobs/lint-gate.yaml defining a job entry that invokes scripts/preflight.sh and references the cadence slot field
  Verification: grep -q 'preflight' jobs/lint-gate.yaml
- [ ] Add tests/test_cadence.py with stdlib-only assertions covering next_slot for cron-like and fixed-interval expressions
  Verification: python3 tests/test_cadence.py
- [ ] Run the full hngh-automation suite to confirm no regressions from the new files
  Verification: make test
