<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
Implements research line synth-2026-09-20-1 by adding an automation-side validation script that enforces the verified UTC timestamp invariants (fixed-width `YYYY-MM-DDTHH:MM:SSZ` shape) for record serialization, ensuring the writer boundary contract is checked independently of the kernel suite.

## Steps
- [ ] Create `scripts/validate-timestamps.py` implementing a stdlib-only regex check for the exact UTC timestamp format defined in `src/main.lisp:55-58`.
  Verification: python3 scripts/validate-timestamps.py --help
- [ ] Add a test fixture file `tests/fixtures/sample-records.txt` containing valid and invalid timestamp lines to exercise the validation logic.
  Verification: grep -q "2026-09-21T12:00:00Z" tests/fixtures/sample-records.txt
- [ ] Extend `scripts/validate-timestamps.py` to accept a file path argument and exit non-zero if any line fails the timestamp invariant.
  Verification: python3 scripts/validate-timestamps.py tests/fixtures/sample-records.txt
- [ ] Add a shell wrapper `scripts/check-serialization.sh` that invokes the Python validator against a specified directory of record files.
  Verification: bash -n scripts/check-serialization.sh
- [ ] Integrate the new check into the existing test suite by adding a call to `make test` target or ensuring it runs via standard automation entry points without modifying kernel files.
  Verification: make test
