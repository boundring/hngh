<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

Implements the ground-truth pass on synth-2026-09-20-1 by codifying the verified byte-level serialization invariants (UTC normalization and s-expression record shape) into a runnable automation check.

## Steps

- [ ] Create `scripts/verify-record-invariants.py` that parses a sample record line to assert it is a valid s-expression and contains a timestamp matching the fixed-width `YYYY-MM-DDTHH:MM:SSZ` regex.
  Verification: python3 scripts/verify-record-invariants.py
- [ ] Add a test fixture file `tests/fixtures/sample-record.txt` containing one valid record line with a correctly formatted UTC timestamp.
  Verification: grep -qE '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' tests/fixtures/sample-record.txt
- [ ] Update `scripts/verify-record-invariants.py` to read the fixture file path from an argument and exit non-zero if the line fails s-expression or timestamp checks.
  Verification: python3 scripts/verify-record-invariants.py tests/fixtures/sample-record.txt
- [ ] Add a make target or script invocation in `tests/run-serialization-check.sh` that executes the Python verifier against the fixture.
  Verification: bash -n tests/run-serialization-check.sh
- [ ] Run the full test suite to ensure the new serialization check integrates without breaking existing gates.
  Verification: make test
