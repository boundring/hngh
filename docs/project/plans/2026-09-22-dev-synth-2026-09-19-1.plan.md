<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

Implements the byte-level serialization invariants from research line synth-2026-09-20-1 by adding an automation-side linter that validates record file shape against the kernel's fixed-width UTC timestamp and s-expression line format.

## Steps

- [ ] Create `scripts/validate-record-format.sh` to parse a sample record file and assert lines are valid s-expressions with a leading timestamp matching `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z`
  Verification: bash -n scripts/validate-record-format.sh
- [ ] Add a test fixture `tests/fixtures/sample-record.txt` containing three valid record lines and one invalid line (malformed timestamp) to drive the linter
  Verification: grep -q "invalid-timestamp" tests/fixtures/sample-record.txt
- [ ] Create `tests/test-validate-record-format.sh` that invokes the linter against the fixture and asserts exit code 1 for the invalid file and 0 for a valid subset
  Verification: bash tests/test-validate-record-format.sh
- [ ] Update `Makefile` to add a `lint-records` target that runs `scripts/validate-record-format.sh` against all files in `jobs/records/` if present, otherwise passes
  Verification: make lint-records
- [ ] Add a CI check step in `cadence/ci.sh` that calls `make lint-records` and fails the pipeline if any record file violates the byte-level invariants
  Verification: grep -q "make lint-records" cadence/ci.sh
