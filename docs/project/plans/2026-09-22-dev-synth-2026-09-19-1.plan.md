<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the byte-level serialization invariants research line (synth-2026-09-20-1) by codifying the UTC normalization and s-expression line format into a standalone, stdlib-only validation script for hngh-automation.

## Steps

- [ ] Create `scripts/validate-record-format.py` that reads a record file and asserts each line is a valid s-expression with a fixed-width `YYYY-MM-DDTHH:MM:SSZ` timestamp field.
  Verification: python3 scripts/validate-record-format.py tests/fixtures/sample-record.txt
- [ ] Add a fixture file `tests/fixtures/sample-record.txt` containing three valid s-expression lines with correctly formatted UTC timestamps and one invalid line for negative testing.
  Verification: grep -q "2026-09-21T12:00:00Z" tests/fixtures/sample-record.txt
- [ ] Extend `scripts/validate-record-format.py` to exit non-zero when it encounters a line that fails s-expression parsing or timestamp regex matching.
  Verification: python3 scripts/validate-record-format.py tests/fixtures/sample-record.txt; test $? -eq 1
- [ ] Add a CI hook in `cadence/ci.sh` that invokes the new validator against all files under `jobs/*/records/` if the directory exists.
  Verification: bash -n cadence/ci.sh
- [ ] Run the full hngh-automation test suite to confirm no regressions from the new script and fixture additions.
  Verification: make test
