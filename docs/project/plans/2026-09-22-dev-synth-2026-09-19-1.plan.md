<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the research line for standardizing job execution logging and failure reporting within the hngh-automation framework to improve observability without altering core kernel or provider logic.

## Steps

- [ ] Create a new bash script at `scripts/log_job_status.sh` that accepts a job ID and status string, appending a timestamped entry to a local log file in `jobs/`.
  Verification: bash -n scripts/log_job_status.sh

- [ ] Add a helper function to `lib/common.sh` that formats the standard error output for failed jobs, ensuring consistent prefixing with "ERROR:" for downstream parsing.
  Verification: grep -q "ERROR:" lib/common.sh

- [ ] Update the existing job definition in `jobs/nightly_build.sh` to source `lib/common.sh` and invoke `scripts/log_job_status.sh` upon completion.
  Verification: grep -q "log_job_status.sh" jobs/nightly_build.sh

- [ ] Create a test script at `tests/test_logging.sh` that executes `scripts/log_job_status.sh` with a dummy ID and verifies the log file contains the expected status string.
  Verification: bash tests/test_logging.sh

- [ ] Integrate the new logging test into the main test suite by adding an execution call to `make test` target in the root Makefile, ensuring it runs before final exit.
  Verification: make test
