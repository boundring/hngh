<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Rationale
This plan implements the research line for standardizing job execution and error handling within hngh-automation by introducing a reusable shell library for safe command wrapping and log aggregation.

## Steps

- [ ] Create `lib/common.sh` containing a `run_cmd` function that executes commands with `set -euo pipefail` and logs output to `[redacted path]
  Verification: bash -n lib/common.sh

- [ ] Add a test script at `tests/test_common.sh` that sources `lib/common.sh`, calls `run_cmd` with `echo "test"`, and asserts the log file contains "test".
  Verification: bash tests/test_common.sh

- [ ] Update `jobs/nightly_build.sh` to source `lib/common.sh` and replace direct command execution with `run_cmd` for all build steps.
  Verification: make test

- [ ] Add a grep check in `tests/test_nightly_build.sh` to verify that `jobs/nightly_build.sh` contains the string "source lib/common.sh".
  Verification: grep -q "source lib/common.sh" jobs/nightly_build.sh

- [ ] Create `scripts/cleanup_logs.sh` that removes log files older than 7 days from `[redacted path]
  Verification: bash -n scripts/cleanup_logs.sh
