<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-22 - dev-synth-2026-09-19-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the byte-level serialization invariants from research line synth-2026-09-20-1, specifically codifying the UTC normalization and s-expression record format enforced by the kernel writer.

## Steps

- [ ] Create `lib/serialization-spec.md` documenting the fixed-width `YYYY-MM-DDTHH:MM:SSZ` timestamp shape and one-form-per-line s-expression record structure.
  Verification: grep -q "YYYY-MM-DDTHH:MM:SSZ" lib/serialization-spec.md
- [ ] Add a shell script `scripts/check-record-format.sh` that validates a sample record file contains only valid s-expression lines without JSON delimiters.
  Verification: bash -n scripts/check-record-format.sh
- [ ] Create `tests/test-serialization-invariants.py` using stdlib to assert timestamp strings match the UTC normalization regex and records parse as lisp forms.
  Verification: python3 tests/test-serialization-invariants.py
- [ ] Update `cadence/verification.sh` to invoke the new serialization check script during the standard test gate.
  Verification: grep -q "check-record-format" cadence/verification.sh
