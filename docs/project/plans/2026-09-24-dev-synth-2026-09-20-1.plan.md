<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-24 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a smoke test for the cadence scheduler that validates job ordering
  Verification: bash -n cadence/scheduler_test.sh && make test
- [ ] Create a helper script that validates job manifest structure before execution
  Verification: bash scripts/validate_manifest.sh && python3 scripts/validate_manifest.py
- [ ] Update cadence runner to log job start/end timestamps to dashboard output
  Verification: make test && grep -q "timestamp" dashboard/output.log
- [ ] Add a unit test for the digest formatter that checks output schema
  Verification: bash tests/test_digest.sh && make test
- [ ] Refactor lib/job_runner to extract retry logic into a reusable function
  Verification: bash -n lib/job_runner.py && make test
- [ ] Add a dashboard view that displays job completion rates over the last 24 hours
  Verification: make test && grep -q "completion_rate" dashboard/views.py
