<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

This plan implements the adopted hngh-automation job-state validation research line by adding small, stdlib-only and shell-based validation helpers under automation paths.

## Steps

- [ ] Add scripts/check_job_queue.sh with a built-in self-test that validates an in-memory job queue snippet and exits nonzero when required fields are missing.
  Verification: bash scripts/check_job_queue.sh
- [ ] Add lib/job_labels.py as a stdlib-only helper that normalizes job status labels and runs a self-test when executed directly.
  Verification: python3 lib/job_labels.py
- [ ] Add tests/fixtures/job_status.json and tests/check_job_fixture.py, where the Python script loads the fixture with stdlib json and fails on malformed structure.
  Verification: python3 tests/check_job_fixture.py
- [ ] Add dashboard/summarize_jobs.py as a stdlib-only summary renderer that builds an in-memory dashboard text block and self-tests without network access.
  Verification: python3 dashboard/summarize_jobs.py
- [ ] Add digest/render_digest.py as a stdlib-only digest builder that renders a plain-text digest from job summaries and self-tests on exit.
  Verification: python3 digest/render_digest.py
