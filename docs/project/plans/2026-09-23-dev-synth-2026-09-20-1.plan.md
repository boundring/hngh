<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a cadence job that runs a simple health check script on a daily schedule
  Verification: bash -n cadence/health-check.sh && grep -q "health-check.sh" cadence/schedule.yaml

- [ ] Create the health check script under scripts/ that outputs a status line
  Verification: bash -n scripts/health-check.sh && head -1 scripts/health-check.sh

- [ ] Register the new cadence job in the schedule configuration
  Verification: grep -q "health-check" cadence/schedule.yaml && make test

- [ ] Add a test for the health check script to ensure it exits cleanly
  Verification: bash scripts/health-check.sh && echo "exit code: $?"
