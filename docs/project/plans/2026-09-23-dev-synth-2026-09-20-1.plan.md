<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a new test job that validates dashboard digest generation end-to-end
  Verification: make test

- [ ] Create a script that checks all dashboard output files are non-empty after digest run
  Verification: bash scripts/digest-check.sh

- [ ] Add a cadence job that runs the digest check script on schedule
  Verification: bash -n cadence/digest-check-job.sh

- [ ] Update the test suite to include a regression check for dashboard file timestamps
  Verification: make test

- [ ] Add a verification script that confirms all jobs/ scripts/ cadence/ paths are syntactically valid
  Verification: bash -n jobs/*.sh && bash -n scripts/*.sh && bash -n cadence/*.sh
