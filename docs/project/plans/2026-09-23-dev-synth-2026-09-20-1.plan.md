<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a job-output validator script under scripts/ that checks recent job artifacts for expected markers
  Verification: bash -n scripts/validate-job-output.sh && make test

- [ ] Create a cadence entry under cadence/ that runs the validator on a fixed interval
  Verification: bash -n cadence/run-validator.sh && make test

- [ ] Add a test under tests/ that exercises the validator with synthetic job output
  Verification: bash -n tests/test-validator.sh && make test

- [ ] Update the dashboard config under dashboard/ to surface validator status
  Verification: grep -q 'validator' dashboard/config.yaml && make test
