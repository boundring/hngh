<!-- plan: status=accepted risk=normal accepted=2026-09-23T23:03:58Z -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a reusable verification helper at scripts/verify_job_output.sh that checks a job's stdout contains expected markers
  Verification: bash -n scripts/verify_job_output.sh

- [ ] Add a test case for the helper at tests/test_verify_job_output.sh that asserts the helper returns 0 on valid input
  Verification: bash tests/test_verify_job_output.sh

- [ ] Update cadence/cadence_runner.sh to invoke the helper after each job completes and log the result
  Verification: bash -n cadence/cadence_runner.sh

- [ ] Run make test to confirm all existing and new tests pass
  Verification: make test

- [ ] Add a grep check confirming no secret patterns or systemd references were introduced
  Verification: git grep -n 'systemctl\|secret\|credential' -- scripts/ cadence/ lib/ tests/ | grep -v 'Verification:' | head -5
