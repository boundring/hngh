<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a new cadence job that runs a daily digest summary generation
  Verification: bash -n cadence/daily-digest.sh

- [ ] Create a helper script that formats digest output for the dashboard
  Verification: python3 -c "import ast; ast.parse(open('scripts/digest-formatter.py').read())"

- [ ] Add a test case that validates the digest formatter output
  Verification: make test

- [ ] Update the cadence runner to include the new daily digest job
  Verification: grep -q 'daily-digest' cadence/runner.sh

- [ ] Verify all new scripts pass syntax checks
  Verification: bash -n scripts/digest-formatter.py && bash -n cadence/daily-digest.sh

- [ ] Run the full test suite to confirm no regressions
  Verification: make test
