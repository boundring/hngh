<!-- plan: status=proposed risk=normal accepted=- -->
# 2026-09-23 - dev-synth-2026-09-20-1 (synthesized from adopted research)

Synthesized by the overnight cycle from verdict=adopted research
dispositions (local chain, pinned); admission via accept-plans.

## Steps

- [ ] Add a job runner script that iterates over pending automation tasks in jobs/ and executes them sequentially
  Verification: bash -n jobs/runner.sh

- [ ] Create a test that validates the runner processes exactly one task per invocation without side effects
  Verification: make test

- [ ] Add a cadence entry that triggers the runner on a fixed interval and logs output to dashboard/
  Verification: bash -n cadence/cadence.sh

- [ ] Write a verification script that confirms dashboard/ receives runner output after cadence execution
  Verification: python3 dashboard/verify_output.py

- [ ] Add a README note documenting the runner's expected behavior and failure modes
  Verification: grep -q "runner" jobs/README.md
