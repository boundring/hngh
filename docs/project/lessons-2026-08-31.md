# Lessons — 2026-08-31 (automatic harvest)

Harvested by lesson-harvest.sh (observe→reflect→improve) at 09:00Z.

## Sources scanned
- Records newer than last harvest: 3
- Watchdog handoff ledger: 103 lines (0 new)
- Guardrails: ~/Projects/etc/hngh/docs/project/agent-guardrails.md

## New lesson-bearing records
- 2026-08-30-evening-selfdev-plan.md
- 2026-08-30-lessons-and-foldback.md
- README.md

## Guardrail cross-check
Does any new record extend a guardrail class or add a new one?
Fold it into agent-guardrails.md — that is the improvement leg.

## Overnight continuity wave (2026-08-30 plan, executed 2026-08-31)

Wrap notes from the plan's step 6, appended by the 21:00Z wake:

- The 20:00Z beat died (rc=124, 30m kill) 14s after landing its
  ceremony commit (11de68c) — before ticking the plan file. The next
  wake had to re-derive four executed-but-unticked steps from the
  ledger before acting. Lesson: tick each step inside the ceremony
  that completes it; a deferred tick costs the next wake a full
  discovery pass.
- Beat sizing must leave room for the ceremony: steps 1–4 plus the
  docs ceremony fit 30m only because each step was small; the wrap
  did not fit and spilled to the next wake. Size a wake's work to
  ~25m so ceremony + push land inside the kill.
- Unread report-queue alerts grew 486 → 499 → 514 across three wakes
  — monotonic, never drained. The routing-table design doc is now
  field-complete (2026-08-31 outcome-tracking extension, six fields),
  so the next plan's grow beat can price and implement the
  router tick on the hngh-automation side (automation commits are
  free — no ceremony needed there).
- The 00:00Z→00:30Z wake (2026-09-01) landed steps 1–4 in ONE beat
  (gates, router tick, research doc, batched ceremony) because each
  step was sized to minutes, not fractions of the beat: the router
  tick was one script + one test file; the ceremony was one
  ceremony-drive invocation (37s). The 21:00Z-era estimate "a
  ceremony cannot fit in a partial beat" was a sizing statement, not
  a law — the law is that a ceremony must COMPLETE inside the beat,
  and hand-driving the CLI is what made old ceremonies slow.
- ceremony-drive is the batched-ceremony instrument for docs
  batches: it runs the whole loop (create-run, admit model, propose
  with deterministic ten-principle verdict, cert-bound prepare +
  commit, certificate-gated push) in under a minute and pushed
  5f0a0a2..1754cb9 unattended. Pre-flight the public-content gate
  (absolute home paths, credential shapes, eval/exec) BEFORE the
  invocation — refusals are cheap, a burned ceremony is not.
- Router tick landed end-to-end (hngh-automation 87e6bc3): closed-step
  re-fire → observable duplicate-skip pair (STATE.md breadcrumb +
  deduped alert row), live-demonstrated against the executed
  2026-08-30 plan (row f9360a6e). Honest remainder: no production
  caller invokes the tick yet — wiring is the next plan's first
  grow beat.
