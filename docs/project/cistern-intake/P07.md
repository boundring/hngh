# P07 — Ambient-lite tick + level-aware spawns (V6-07)

STATUS: QUEUED — wave 2. Gate: P04 (needs per-level hazard lists) landed;
must land BEFORE P08 (spawns anchor to nest/gate). Parallel to the V6-05/06
chain once P04 is in.

## Mission

Ambient-lite tick + level-aware spawns (W3.3, W5.2). ONE
`cistern--ambient-tick` slot appended after rewards-eval: hazard
spread/decay + flood + contamination per off-level, level-id order,
overworld no-op; workers/toilets/migration/raids/story clocks PAUSED
off-level (fairness rule); `cistern--edge-spawn-cell` gains a level-aware
variant (overworld anchors nest/gate; mine keeps edge behavior).

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §1.1 ambient-tick slot, §1.2 rules 3/7, §3 row V6-07,
  §4 guard 8
- `docs/v6/WORLD.md` §W3.3, §W5.2
- `src/cistern-domain.el` (tick pipeline tail), `tests/domain-tick.el`,
  `tests/domain-determinism.el`

## Constraints

- PROTECT notes (verbatim): no-unseen-deaths fairness rule; ambient draws =
  trajectory (level switches included); cost = list walk, not map scan.
- Ambient-tick is LAST in the tick (R-4: off-level events land next-tick
  visible; the servant acts on the level in view).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO3.2 (off-level hazard event logs level-tagged; ZERO worker
movement/toilet/death off-screen); WO5.1 ambient-draw half.

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
