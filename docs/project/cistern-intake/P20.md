# P20 — Browser channels (V6-20)

STATUS: QUEUED — wave 3, lane B. Gate: P18 + P19 (filter 3 has content only
once the summarizer exists).

## Mission

Browser channels (LOGS §3.1). Additive keys `1`/`2`/`3`/`0` on the ONE
`*cistern log*` buffer; per-channel `log-header-*` variants + `log-hint`
extension; staleness gate becomes cons `(ring-length . channel)`; `3`
re-derives the summary on build.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-20 + §4 guard 9 (S5)
- `docs/v6/LOGS.md` §3.1, §6.1
- `src/cistern-view.el` (log browser keymap), `tests/` browser probes

## Constraints

- PROTECT notes (verbatim): L-browser keymap ADDITIVE ONLY; S5 (never
  gates).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

LG8 (old ∩ new keymap identical actions; n/p//g/G/RET/q probe green); LG9
(filters isolate; 3 re-derives; 0 = v5 view); LG10 (RET jumps per channel
incl. summary anchors; point survives same-channel round trip).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
