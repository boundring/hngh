# P04 — Level record + st restructure (V6-04)

STATUS: QUEUED — wave 2. Gate: wave 1 complete (V6-01/02/03 landed).

## Mission

Level record + st restructure (WORLD W3.2 state shape). `cistern-st` gains
`:levels` alist + `:cur-level`; new `cistern--level` struct (id, w, h, map,
workers, hostiles, toilets, tanks, hazard, event-tiles, contam, particles,
cursor); every query/sim boundary passes `(cistern-st-cur st)`; st stays
global for seed, tick, alloy, log, story, personas, relationships,
rewards-events, goals.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140. Wave-2 packets claim
  from the ACTUAL post-wave-1 state (reconciliation probes first).

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-04 + cross-wave dep note + §4.1 item 4
- `docs/v6/WORLD.md` §W3.2
- `src/cistern-game.el`, `src/cistern-domain.el`, `tests/` (whole suite is
  the net — this is the one structural change)

## Constraints

- PROTECT notes (verbatim): D6 query contract unchanged for the view;
  personas global (bound to worker ids).
- V6-04's struct is the gate for everything level-shaped below it (V6-05..10).
- `st.theme` (P03) moves into the level record here (L-112 item 4 note).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

Existing suite green on a single `mine-1` level; struct round trip (levels
preserved through switch/switch-back).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
