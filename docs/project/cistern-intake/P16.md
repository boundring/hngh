# P16 — Meta-automation (V6-16)

STATUS: QUEUED — wave 3, lane A. Gate: P15 (the meta layer runs inside
V6-15's slot). Order: engine → meta → UI.

## Mission

Meta-automation (CONTROLS §5). Meta-rules as a second plist list;
`meta-eval-every` 10 ticks at the HEAD of automation-eval; `:tune` carries
exactly `:threshold`/`:cooldown`/`:enabled` of exactly one existing rule id;
pinned bands (thresholds 50–95 / 1–limit / 1–4, cooldown 0–20) with
clamp-once logging (`meta-clamped`); unknown id refused at load
(`meta-unknown-rule`); no creation/deletion; two tuners of one param → last
in file wins; `:during-raid` hold/run, default hold.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-16 + §4 guard 11 (meta bands pinned)
- `docs/v6/CONTROLS.md` §5 (5.1–5.4), §6
- `src/cistern-domain.el` (automation-eval head), `data/rules.el` shape,
  `tests/` automation tests from P15

## Constraints

- PROTECT notes (verbatim): meta ≤ meta (no recursion); story never reads
  meta; comedy may READ automation-fired events (bank content, not engine).
- Pinned bands are PINNED (§4 guard 11) — no "improving" them later.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

MM1 (band clamp + one clamp log); MM2 (unknown id never fires; no add/delete
form structurally); MM3 (last-in-file deterministic); MM4 (hold through
raid, resume at close).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
