# P02 — Header strip reflow — priority elision (V6-02)

STATUS: LANDED — commit beb3fff (ledger L-111). Acceptance-evidence example:
executed by the director's run immediately after P01; kept as the second
worked shape (note the Q11 audit collision caught mid-flight — a real ruling
harvested into the ledger, not a silent fix).

## Mission

Header strip reflow — priority elision (W1.4). Header segments become an
ORDERED list (tick/clock/level name → goals/pressure → badge → help →
legend); strip builder keeps what fits, elides deepest-priority first;
permanent-row count FIXED (3 + ≤1 badge + ≤2 help + ≤2 legend) so
`:header-lines` stays truthful; pressure words S2-protected: dropped, never
rewritten.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- HEAD d638e3f, tree clean, suite canonical 140/140 (post-V6-02 halt state).

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 1 row V6-02 + §4 guards 5 (C1')
- `docs/v6/WORLD.md` §W1.4–W1.5
- `src/cistern-view.el`, `tests/test-v6.el`

## Constraints

- PROTECT notes (verbatim): S2 voice; L-014 (`:header-lines` truthful); C1' —
  elision changes content, never block height.
- Copy-table rule (Q11): any new player-facing string lands in
  `cistern--copy` under a named key at design time.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO1.3 (no permanent row exceeds a narrow synthetic width; pressure lines
byte-identical when present).

## Outcome

LANDED. Red: "row over 60 cols: 85". Green: ordered priority list
(TICK→ALLOY/POP→CONTAM→SCORE/GOALS/REP) via `cistern-view--strip-elide`;
byte-identical concat at 95+ cols — C1' supersession closed. Suite 140/140
(139 + header). Commit beb3fff; ledger L-111 (Q11 pointer-copy collision
fixed: values now "? — HELP/GLYPH BRIEFING"). GREEN-BOUNDARY SYNC done.
