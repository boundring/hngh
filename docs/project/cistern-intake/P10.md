# P10 — Feral entities + predator-prey valve (V6-10)

STATUS: QUEUED — wave 2, ecology lane. Gate: P04 (per-level reads +
level-aware edge spawn). Otherwise independent of V6-05..V6-09 — may run
parallel to them. Ecology V6-10 → V6-11 → V6-12 strictly sequential.

## Mission

Feral entities + predator-prey valve (ECOLOGY §1–2, §5). Faction `feral` on
the EXISTING entity struct; vent cat `k` (HP 6) / sector dog `d` (HP 8),
stream-4 dossiers, ATK = FLOW mod, DEF = 10 + GRIT mod; shared strike
pipeline (dmg-minor matrix); P = 2·(live rats) + severed; cat arrival every
`feral-every` 20, d20 vs `clamp(20 − P, 6, 21)`, cap `feral-max` 2; dog
every `dog-every` 240, NO arrival draw, never strikes; food clock on the
existing `idle` slot (patience 60/40, forage 4); rat flight priority-0
within `feral-scent` 2, NO draw, edge despawn; feral filtered OUT of
auto-defense, FOCUS refuses `ecology-refusal` no-draw; hostiles-phase TAIL
gated by `cistern-ecology-enabled`; new consts in one
`cistern--ecology-const` block; ZERO new state fields.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §1.1 hostiles TAIL, §3 WAVE 2 row V6-10, R-5, R-7,
  §4 guards 2, 3, 4
- `docs/v6/ECOLOGY.md` §1–2, §5, §6.1 (EC1–EC9, EC13)
- `src/cistern-domain.el` (hostiles phase, entity struct, auto-defense),
  `tests/test-v5.el` (combat fixtures)

## Constraints

- PROTECT notes (verbatim): stream 4 tail-pinned (v5 fixture order holds);
  ecology-off byte-identical; `feral-*` pushes non-draining; cats cull
  fauna, never write a worker number.
- Ecology is NOT a phase: hostiles-phase TAIL segment, gated
  `cistern-ecology-enabled` (default nil), per §1.2 rule 1 / R-5.
- New glyphs `k d` pass the L-076 gui probe, batch-skipped.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

EC1–EC9, EC13 (k/d glyphs).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
