# P17 — Rules browser (V6-17)

STATUS: QUEUED — wave 3, lane A closer. Gate: P16 (the meta layer exists to
display). Order: engine → meta → UI.

## Mission

Rules browser (CONTROLS §4.8). `C-c C-a` read-only temp buffer in the
`cistern-log-mode` pattern — one line per rule `ID WHEN THEN fires/refused
held?` + copy-table header; keys `e`/`RET`/`g`/`q`; `C-c C-e` opens
`data/rules.el`; on close the driver reloads, diffs into ST, posts `RULES
RELOADED — N ACTIVE`.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-17 + §4 guard 9 (S5)
- `docs/v6/CONTROLS.md` §4.8
- `src/cistern-view.el` (log-mode pattern), `src/cistern.el` (driver
  reload), `tests/` (browser non-modality probes)

## Constraints

- PROTECT notes (verbatim): S5 non-modal; authoring stays in the FILE;
  copy-table rule.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

Batch: reload diff into ST + line render + `e` toggle; S5 probe (a tick
fires while open).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
