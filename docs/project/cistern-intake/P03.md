# P03 — Size classes + sector themes + new-game signature (V6-03)

STATUS: QUEUED — head packet. Gate: V6-01 landed (satisfied). Parallel-safe
against V6-02 per V6-SPEC §3 wave-1 note. This is the recovery packet named
by cistern ledger L-112; its recovery block is carried verbatim in QUEUE.md.

## Mission

Size classes + sector themes + new-game signature (W2).
`cistern--new-game (&optional seed size theme)`, defaults nil → seed
1/standard/standard (today's invocations byte-identical); classes standard
34×16 / large 51×24 / vast 68×32; one `cistern--sector-themes` defconst of
(name . plist) rows read by `cistern--gen-map`; wet's spread multiplier the
one new domain constant, scoped by theme row; NO position indexes.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- HEAD d638e3f, tree clean, suite canonical 140/140 (the L-112 halt state).

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 1 row V6-03 + §4.1 item 3
- `docs/v6/WORLD.md` §W2.1, §W2.2, §W6 (WO2.1/WO2.2)
- `docs/FAILURE-LEDGER.md` L-112 (recovery block — the working plan)
- `src/cistern-game.el` (`cistern--new-game`, `cistern--gen-map`),
  `src/cistern-view.el` (LAY `:body-cols` consumption), `tests/test-v6.el`,
  the soak runner (`cistern-run-soak`)

## Constraints

- PROTECT notes (verbatim): L-011 perf envelope recorded, upgrade path (scan
  indexes) NOT built; size/threading only — no per-class procgen engines.
- Byte-identity anchors + theme knob plan + doc-drift ruling: carried
  verbatim in `QUEUE.md` halt-state summary (from L-112 items 2–4). The
  theme/size knobs MUST NOT add or remove a sim-LCG draw on the standard
  theme (extras gated on knob > 0, appended after gen-map's existing draws).
- Ruling to carry: WORLD W2.2's "nil → seed 1" loses to the code default
  20260830 — code is the pinned reading; fix the doc in the green commit.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md` (incl.
  check-parens after every elisp edit — L-017 class, cost four debug cycles
  in L-110..L-112 when skipped).
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO2.1 (vast bounds 68×32; 600-tick vast soak ≤ 4× standard wall time); WO2.2
(same seed + different theme ⇒ different hash, same bounds; wet flood-seed
count seed-invariant). Byte-identity anchors (seed 1/42/20260830) hold
unchanged on the standard theme.

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
