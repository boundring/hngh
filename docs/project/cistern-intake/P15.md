# P15 — Automation engine (V6-15)

STATUS: QUEUED — wave 3, lane A. Gate: P14 (verb set + key data stable);
lane A order V6-15 → V6-16 → V6-17.

## Mission

Automation engine (CONTROLS §4). New ST slot `(automation nil)`; rules as
plists in `data/rules.el`; condition vocabulary = EXISTING queries only
(read-without-drain, story's `cistern--story-condition-p` shape); closed
SIX-entry action table → `cistern--cmd-build/demolish/decon/purge/rally/
focus`; ONE slot pinned sim-phases → **automation-eval** → story-eval; ≤ 1
action/tick sector-wide; per-rule `:cooldown`; 10-tick refusal hold
(`automation-refusal-pause`); `(automation fired/refused …)` pushes;
automated build advances NO extra tick (inside the clock, L-010).

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §1.1 automation slot, §1.2 rule 2, §3 WAVE 3 row
  V6-15, §4 guards 2, 3, 7
- `docs/v6/CONTROLS.md` §4 (4.1–4.7), §6
- `src/cistern-domain.el` (do-tick slot), `src/cistern-game.el`
  (cmd-* verbs, story-condition-p), `tests/game-rewards-consumption.el`
  (drainer contract)

## Constraints

- PROTECT notes (verbatim): servant invariant; zero stream draws (rng
  positions tick-for-tick vs rules-free run); drainer contract → SIX
  non-draining readers; S3 (same popup-at-act path), S4 (cmd-purge invoked,
  not re-implemented).
- Meta-eval will run at this slot's HEAD (P16); pin the hook point now.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

AU1 (pipeline: fire-tick list; events visible same tick, drained only by
rewards-eval); AU2 (servant delta identity ×6); AU3 (cap + list order); AU4
(one refusal line, 10-tick hold, no retry); AU5 (vocabulary ceiling + no
tick/drain/story mutation); AU6 (soak survival); DT1/DT2; SP1–S5 probes.

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
