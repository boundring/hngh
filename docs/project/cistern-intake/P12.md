# P12 — Ecology surfaces — copy + inspector (V6-12)

STATUS: QUEUED — wave 2, ecology lane, wave-2 closer for the lane. Gate:
P11 landed.

## Mission

Ecology surfaces — copy + inspector (ECOLOGY §4). `(ecology . …)` copy
subsection, the ten §4 keys ≤ 60 raw chars; animals ride
`combat-inspect-fmt` with state words `ON PATROL` / `ON ROUND` /
`DEPARTING` (past patience − 10); `feral-arrival`/`feral-depart` events feed
the hook machine's closed grammar.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-12 + §4 guards 6, 9, 10
- `docs/v6/ECOLOGY.md` §4, §6.3 (soar protection per soar)
- `src/cistern-game.el` (combat-inspect-fmt), `src/cistern-view.el`,
  `tests/` (CY12-style copy sweeps)

## Constraints

- PROTECT notes (verbatim): S1 (no new query path); S2 (info severity, never
  the PRESSURE family); comedy-violence contrast (predation not an anchor;
  CY12 scan covers the new subsection).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

EC13 (width), EC14 (copy sweep — grep finds no ecology literal in
view/game).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
