# P05 — Overworld gen + tile vocabulary (V6-05)

STATUS: QUEUED — wave 2. Gate: P04 (struct) landed; strictly V6-04 → V6-05
→ V6-06.

## Mission

Overworld gen + tile vocabulary (W3.1, W5.1). Five tile-table rows
`gantry =` / `elevator ▼` / `gate Π` / `settlement Λ` / `nest Ψ` (+ mine `▲`
pairing); one overworld gen pass 40×12 on the sim LCG, AFTER the mine level
(order pinned); copy keys `nav-no-elevator`, `nav-no-shaft`,
`unit-browser-header`, `legend-/desc-/insp-` ×5, `log-level-fmt`;
duplicate-key copy-loader guard lands here (R-3).

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-05 + §4 guards 3, 4, 6
- `docs/v6/WORLD.md` §W3.1, §W5.1, §W5.3
- `src/cistern-game.el` (gen, copy loader), `tests/domain-tile-table.el`,
  `tests/domain-procgen.el`

## Constraints

- PROTECT notes (verbatim): S3.1/S3.2 sweep pattern; faces from S2 roles
  (nest alert, elevator emphasis); C2 named-at-design-time.
- L-076 pin route: the five new tile glyphs + `▲` pass the gui probe
  (`font-at` advance == cell width), batch-skipped.
- Gen order pinned: mine-then-overworld (W5.1) — the whole LCG trajectory
  rests on it.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO5.1 gen-order half (same (seed,size,theme) ⇒ same two maps); glyph gui
probes.

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
