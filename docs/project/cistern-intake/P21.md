# P21 — Tail tags + banner summary + ELSEWHERE (V6-21)

STATUS: QUEUED — wave 3, lane B closer / v6 closer. Gate: P18 (tags) and
P19 (banner content). Last packet — wave-3 close review runs the six-item
checklist + per-wave grep sweep + soar re-probes (§4 guard 9).

## Mission

Tail tags + banner summary + ELSEWHERE (LOGS §3.2, §4). Tail pick rule
extends (errors → ambient by recency → story in leftover slots); `[A] `/`[S]
` 3-col prefixes; banner row default = LATEST summary entry, dim +
severity-faced, ceremony overrides; `cistern-view--in-viewport-p` +
`pressure-elsewhere` suffix — first Tier-1 standing problem in row-major
order outside the viewport, from LIVE state queries (no ring scans).

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-21 + cross-wave dep (V6-01's
  ELSEWHERE viewport helper) + §4 guards 5, 9
- `docs/v6/LOGS.md` §3.2, §4 (4.1–4.2), §6.1
- `src/cistern-view.el` (tail render, banner row, viewport),
  `tests/` (v5 tail byte-identity fixtures)

## Constraints

- PROTECT notes (verbatim): S2 (suffix via copy table only, never edited);
  S3 (one banner row, zero geometry); S5; C1' width on every row.
- ELSEWHERE helper is inert on a whole-sector render; activates under any
  viewport smaller than the map (LOGS §4.2).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

LG11 (cold: pure-ambient tail AND pressure lines byte-identical to v5);
LG12 (ceremony overrides; summary shows when quiet); LG13 (ELSEWHERE inert
on 34×16 whole-map render, activates under a forced smaller viewport).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
