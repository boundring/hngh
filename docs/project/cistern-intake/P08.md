# P08 — Faction homes — warband march, guild settlement (V6-08)

STATUS: QUEUED — wave 2. Gate: P05 + P06 (march needs tiles + switches).

## Mission

Faction homes — warband march, guild settlement (W3.3). Raid opens with the
warband spawning at the overworld `nest`, marching to the nearest elevator,
descending, then the EXISTING raid lifecycle in the mine unchanged;
`cistern--maybe-guild` spawns the fixer walking from the `settlement` into
the active mine; actors cross levels only as authored events.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-08 + §4 guard 8
- `docs/v6/WORLD.md` §W3.3
- `src/cistern-domain.el` (raid open, maybe-guild), `tests/test-v5.el`
  (raid lifecycle tests stay green)

## Constraints

- PROTECT notes (verbatim): combat pipeline untouched below the spawn point;
  S5 (off-level ceremony = announcement + log tag).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO3.3 (staged raid: nest → elevator → mine, existing lifecycle asserts
green).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
