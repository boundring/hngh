# P13 — Keymap overhaul (V6-13)

STATUS: QUEUED — wave 3, lane A head. Gate: wave 2 complete. Lane A order:
V6-13 → V6-14.

## Mission

Keymap overhaul (CONTROLS §2). Three rules K1/K2/K3; six moved keys: `K`→
`k`, cycle→`TAB` (`T` kept as alias), `H`→`R`, `n`→`N`, `C-t`→`C-c C-t`,
`C-x z` bound for real; `u` removed (C-g/ESC disarm); four one-shot coach
lines through the Q17 slot + first-session banner, copy-table keys.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-13 + R-8 + §4 guard 6
- `docs/v6/CONTROLS.md` §1, §2 (2.1–2.3)
- `src/cistern-input.el`, `tests/test-r2-keymap.el`, `tests/test-ux-r2.el`

## Constraints

- PROTECT notes (verbatim): emacs pairing layer, SPC/RET tick, coach + hint
  channels untouched; A8 softened by copy, not machinery (L-010/Q19 intact).
- `u` removal must not break the unit browser binding wave-2 P09 installs —
  reconcile at claim; R-8 nav keys do NOT collide (unbound punctuation).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

KM1 (`k`/`TAB`/`R`/`N`/`C-c C-t` bound; `K`/`H`/`n`/`C-t`/`u` UNBOUND); KM2
(`C-x z`); KM3 (briefing: case rule, C-s isearch framing, no `%%`); KM4
(coach fires once).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
