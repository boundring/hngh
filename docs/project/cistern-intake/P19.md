# P19 — Summarizer (V6-19)

STATUS: QUEUED — wave 3, lane B. Gate: P18 (entry shape). Parallel-safe
after P18.

## Mission

Summarizer (LOGS §2). Pure `cistern--log-summarize (log st)` → ordered
`(TICK SEV TEXT)` entries; windows pure in tick + recorded act-open ticks,
cadence `cistern-summary-every` 60; quadrant clustering, FIXED clause order
(breach → losses → raid → income → QUIET); `(summary . …)` copy section +
`sector-nw/ne/sw/se`.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §1.2 rule 6, §3 WAVE 3 row V6-19, §4 guard 3
- `docs/v6/LOGS.md` §2 (2.1–2.3), §6.1 (copy keys)
- `src/cistern-domain.el` (act ticks, log ring), `tests/`
  determinism fixtures

## Constraints

- PROTECT notes (verbatim): R2: derived, never stored — zero appends; width
  probe (templates ≤ 60 raw, rendered ≤ live width).
- Zero stream/rng consumption tick-for-tick (LG7; §4 guard 3).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

LG5 (unit fixture + 300-tick fixture byte-exact); LG6 (clause order,
open-vs-contained breach, quiet fallback, act prefix); LG7 (zero
stream/rng consumption tick-for-tick).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
