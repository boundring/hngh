# P14 — Remap system (V6-14)

STATUS: QUEUED — wave 3, lane A. Gate: P13 (the data table needs the final
verb set).

## Mission

Remap system (CONTROLS §3). `cistern--key-table-default` (the §2.2 map as
data, incl. `nav-descend`/`nav-ascend`); pure `cistern--key-table-merge` +
`-keymap-from-table` + `-key-collision-p` (R1 live shadow / R2 noop / R3
prefix / R4 reserved); `data/keys.el` + `cistern--keys-example` +
`cistern--ensure-keys`; `C-c C-r` completing-read flow writes the file on
commit.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 3 row V6-14 + §4 guards 6, 9
- `docs/v6/CONTROLS.md` §3 (3.1–3.4)
- `src/cistern-input.el`, `src/cistern.el` (install path), `tests/` keymap
  tests

## Constraints

- PROTECT notes (verbatim): no second help surface (describe-mode);
  unknown-verb tolerance so a v7 rename cannot brick loading; driver-owned,
  never in `cistern-st`.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

RM1 (every verb bound exactly once; shared-key merge error); RM2 (all four
refusal branches); RM3 (persistence round trip; unknown verb skipped with
one log line); RM4 (`nil`/`""` → `must-keep-key`).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
