# P01 — LAY layout object + camera + click geometry (V6-01)

STATUS: LANDED — commit d059270 (ledger L-110). Acceptance-evidence example:
this packet was executed by the director's run before the Hngh handoff; it is
kept as the worked shape of a packet's life cycle.

## Mission

LAY layout object + camera + click geometry (WORLD W1.1–W1.3). Pure
`cistern-view--layout (body-cols body-lines map-w map-h)` → plist `:map-origin
:map-cols :map-lines :cam :header-lines :legend-rows :help-row`; camera
`clamp(cursor − viewport/2, 0, map-dim − viewport)` recomputed every render;
`cistern-view--cell-at` becomes `(st lay line col)` reading ONLY LAY;
`window-size-change-functions` callback re-derives and re-renders; ZERO stored
geometry.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- HEAD d638e3f, tree clean, suite canonical 140/140 (post-V6-02 halt state).

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 1 row V6-01 + §4 guard 5 (C1')
- `docs/v6/WORLD.md` §W1.1–W1.3, §W1.5
- `src/cistern-view.el`, `src/cistern-input.el`, `tests/test-r1-click.el`,
  `tests/test-v6.el`

## Constraints

- PROTECT block: `docs/v6/V6-SPEC.md` "Carried constraints" — C1' single
  source, two consumers; no layout-state cache (R2-Q02); theme-refresh
  cell-width pin reused, no new machinery.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md` (P3..P5).
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO1.1 (whole-sector at (120,40) + camera clamp at (80,24)); WO1.2 (cell
round-trip through the SAME LAY at three synthetic sizes); WO1.4 (GUI probe:
resize ×3, click remap).

## Outcome

LANDED. Red: 2/139 FAILED (void-function cistern-view--layout). Green: one
derived LAY shared by refresh + click; driver `cistern--current-lay` single
derivation; nil-LAY render default 200×60 synthetic. Suite 139/139 at close
(136 + 3). Commit d059270; ledger L-110. GREEN-BOUNDARY SYNC done.
