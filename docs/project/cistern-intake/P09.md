# P09 — Unit browser (V6-09)

STATUS: QUEUED — wave 2. Gate: P04 (rows are per-level). Parallel-safe
within wave 2 after P04.

## Mission

Unit browser (W4). `u` opens a second special-mode buffer reusing the
log-browser pattern verbatim; one row per worker/enemy — glyph, name, mood,
hp, location — from the same domain queries the inspector uses; `q` closes,
game never blocked.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-09 + §4 guard 9 (S1, S5)
- `docs/v6/WORLD.md` §W4
- `src/cistern-view.el` (log-browser pattern), `src/cistern-game.el`
  (inspector queries), `tests/` browser-mode tests

## Constraints

- PROTECT notes (verbatim): S1 (rows table-driven from domain queries); S5
  non-modal; rejected: tabulated-list.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO4.1 (row-per-entity opens; `q` leaves the game non-modal — a tick fires
while open).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
