# P06 — Navigation verbs + cross-level cursor (V6-06)

STATUS: QUEUED — wave 2. Gate: P05 (tiles exist to descend/ascend on);
order V6-04 → V6-05 → V6-06.

## Mission

Navigation verbs + cross-level cursor (W3.2). `>` descends on an overworld
`elevator`, `<` ascends on a mine `▲`; both ride the existing adapter chain;
refusal copy via the existing refuse path; clicking an elevator arms descend
(`.` repeat arm carries it); `log-jump-to-source` switches `:cur-level` when
the source is elsewhere.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-06 + R-8
- `docs/v6/WORLD.md` §W3.2
- `src/cistern-input.el`, `src/cistern-view.el` (jump-to-source),
  `tests/game-cursor.el`

## Constraints

- PROTECT notes (verbatim): S5 non-modal — level swap is one re-render of
  the same buffer; cursor remembered per level.
- R-8: `>`/`<` join the motion group as `nav-descend`/`nav-ascend` in the
  remap table (V6-14's data) — they ride the keymap data table from day one.
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

WO3.1 (`>`/`<` round trip: both levels' maps, workers, hazard counts
preserved; overworld cursor restored at the elevator).

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
