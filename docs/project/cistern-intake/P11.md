# P11 — Self-balancing sweep (V6-11)

STATUS: QUEUED — wave 2, ecology lane. Gate: P10 landed (strictly
V6-10 → V6-11 → V6-12).

## Mission

Self-balancing sweep (ECOLOGY §3). Wealth valve: W = tank load total +
2·banked alloy, `n = clamp(1 + floor(W/15), 1, min(act-cap, pop − 1))`,
raid-open log gains `ecology-raid-claim`; guild triage: cadence 20 while
severed ≥ 3, restorations 5 while severed ≥ 4 (v5 numbers become FLOORS),
edge-logged `ecology-guild-escalate`; comedy gap `60 + 40·T` cap 180,
calm-40 unchanged; migrant gate 20 while pop ≤ 3, edge-logged
`ecology-gate-valve`/`-normal`.

## Repo state (at queue time)

- Location: `/home/bricker/Projects/etc/20260830/cistern`
- At queue: HEAD d638e3f, tree clean, suite 140/140; reconcile at claim.

## Files to read

- `docs/v6/V6-SPEC.md` §3 WAVE 2 row V6-11 + §4 guard 11 (pinned NOT
  self-balanced)
- `docs/v6/ECOLOGY.md` §3 (all subsections incl. §3.6)
- `src/cistern-domain.el` (maybe-raid, guild, comedy gap, migrant gate),
  `tests/` (v5 equilibrium fixtures)

## Constraints

- PROTECT notes (verbatim): PINNED, not valves: P1 hard valve (contam ≥ 18),
  `raid-span` 40, `hostiles-max` 8, pop-cap 8, `spread-pct`/`decay-pct`, act
  windows 120/240, raid DC act scaling 8→5 — valves modulate rates, never
  caps; no hidden rubber-banding (announced, pure reads).
- New copy keys named at design time in the `(ecology . …)` subsection
  (Q11/C2; duplicate-key loader already live from P05).
- Standing checklist + probe rules: `docs/HANDBRIEF-TEMPLATE.md`.
- Failure protocol: `docs/FAILURE-LEDGER.md`; death-and-replacement per
  `hngh docs/project/roguelike-agentic.md`.

## Done-when

EC10, EC11, EC12.

## Outcome

(claimed <date> by <cycle>; commits; ledger L-0NN; re-brief notes)
