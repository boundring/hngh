# 2026-08-10 — K3 review consumption and workspace migration

## Quota evidence

The operator's Kimi quota screenshot, read locally, showed the one-shot K3
review consumed 72.97% of the 5-hour window and 14.71% of the 7-day window;
the page showed 23.88% total usage. The 5-hour window later reset. This is
useful authority work at the wrong routine size.

## Method changes

K3 artifact 95 is accepted as verified input. Its findings were converted into
project mechanics rather than re-investigated:

- state claims require evidence artifacts;
- completion triggers claim release and lane/deck reconciliation before rest;
- raw tmux steering remains prohibited in favor of the delivery adapter;
- K3 runs become one-turn, no-tools authority completions with compact packets;
- 5h/7d/30d envelopes all gate admission; Seu's bounded review found the
  shipped predicate is not yet authoritative (no effective 5h default,
  marginal amount ignored, no ledger rollup/call reservation), so card 128
  hardens quota truth before card 127 wires the planner;
- K3 output is not complete until each finding becomes a card, design change,
  guardrail, or explicit rejection;
- a Pi-like thin harness is a feasibility spike, not a dependency.

## Migration decision

The operator corrected the workspace target. Preserve the complete trees at:

- `~/.hngh/.hngh-night/`
- `~/.hngh/.hngh-day/`

Old roots become compatibility symlinks. The prior flattening design is
superseded. Card 125 assigns Cibo only the fixture-backed migration script;
Killy verifies and runs the live wave after the seats reach a phase boundary.

## Model economy and context lifecycle

Operator policy now treats every remote route above $0.20/M input as reserve,
regardless of provider or output price. Sol's approximately $15 session and the
K3 72.97%-of-5h drawdown showed why broad prompts and long continuing sessions
must be gated. Hermes configuration now disables media/browser/voice/GUI
surfaces for new worker sessions, removes Blender/Unreal/NotHumanSearch MCPs,
and removes OpenCode's NotHumanSearch + unfiltered DepScope MCPs (leaving
local MisakaNet), uses 18% compression with a 10% target,
limits history/turns, and removes reserve routes from automatic fallbacks.

A fresh Cibo/Luna screenshot still showed 72.6K context, 48 tools, 74 skills,
and 4 MCP servers after two minutes. This is the baseline for card 130's
capability audit and cards 131/132's component ledger + compact handoff work.
Offline `prompt-size` then measured: default fixed prompt 93.0KB,
`hngh` 33.8KB before dynamic MCP schemas (32 Hngh skills, no generic
memory/profile/session recall, terminal/file/skills/context only), and
local-only `hngh-minimal`
20.4KB (Unsloth Gemma, no remote fallback, terminal/file/code-execution only).

## Migration result

Card 125 landed at `2de5875` and the live move completed. Scratch fixture:
70 checks. The live move preserved 316 night files and 33 day files with
matching relative hashes/link manifests. Old roots are exact compatibility
symlinks; the watcher restarted active at PID 460980; lane reads work through
both path forms. Full `make test`: 19 suites, 976/976 pass, 0 fail, 0 skip
on code at `2de5875` (docs-only completion record dirty during the run). The
canonical state repo had pre-existing dirty runtime state,
so backup capture remains a bounded follow-up rather than an indiscriminate
commit.

## Coordination

Cibo: implementation-only card 125, exact script/test files, no live mutation.
Seu: bounded review artifact only after the operator assigned Killy design and
direction. Killy owns designs, roadmap, acceptance, and the migration gate.

Attribution: operator, K3 artifact 95, Killy/openai-api gpt-5.6-sol.
