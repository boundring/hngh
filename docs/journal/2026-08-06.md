# 2026-08-06

## M9 resume — mandate-to-code sync + plugin wiring (M9.5, `9116f94`)

- Code-embedded model chains moved off the old kimi/gemma routing to the
  post-mandate chains; Wave 2-4 plugins (file-watcher, squad-dispatch,
  beans, squad-resources) wired into the daemon lifecycle; C3 inference bug
  (rule negation/remote-primary) fixed.
- Makefile `LISP_FILES` glob fixed to include `src/plugins/*.lisp` — plugin
  edits now retrigger binary rebuild (was silently building stale).
- 207/207 fast, 1416/1416 full.

## W5 — prompt matrix build (M9.6, `9e86a90`)

- `generate-prompt`: full dimensional prompt generator — 36 skeletons,
  deterministic bone fillers, optional flesh pass, per-role model selection
  synced to D-040, prompt cache. Backward-compatible with C7
  `generate-pm-prompt` (all C7 tests pass unchanged).
- 390/390 fast, 1591/1591 full.

## Free-tier refresh + model benchmark sourcing (M9.7/M9.8)

- `758ee24` free-tier refresh: replaced stale single-vendor free fallback
  with live OpenRouter catalog IDs across a mixed-vendor set; Qwen-
  AgentWorld-35B promoted to primary local fallback; VRAM/select-system
  mapping updated.
- `f7a3f5b` benchmark-sourcing design brief: HF Open LLM Leaderboard v2,
  lm-eval/promptfoo/local harnesses, ollama timing fields, sysfs VRAM,
  snapshot schema. Verified on-host.

## What's next

- Model probe runner (M9.9); public-release close-out (M9.10); autonomy
  research (M9.11); C6 planner waves + quota-spreader; live-orchestration;
  ACP A1-A4.