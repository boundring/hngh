# 2026-08-07

Heavy day — model probes, public close-out, autonomy research, C6 planner
waves, quota-spreader, live-orchestration, and the ACP A1-A4 stack.

## Model probe runner (M9.9) — `b7ab4ab`

- `data/model-probes.lisp` `run-probe`/`run-probe-suite`: ollama native vs
  OpenAI-compatible endpoints, curl+sb-ext+jsown, per-probe timing
  (tokens/sec, prefill ms), dated snapshot + sysfs VRAM + weighted aggregate.
  Latent broken syntax throughout fixed (`[...]` pseudo-literals,
  unbalanced defparameter, export shape). 494/494 fast, 1687/1687 full.

## Public-release close-out (M9.10) — `1c693dc`

- README + next.md brought to M9 reality (both were stale in lockstep at
  M0-M7); 5 tracked session/journal records scrubbed of `/home/bricker`;
  dual-push verified (github + codeberg after pushurl fix). Pushed both
  remotes.

## Autonomy-strategy research (M9.11) — `78cfde9`→`4317cfc`

- 6-thread synthesis: self-developing/self-healing engine, security-first
  before networking, automated consulting workforce, peer instances. Folded
  delayed SWE/self-dev report in. See `docs/design/autonomy-strategy.md`.

## C6 planner waves + quota-spreader (M9.12)

- `e173d21` Wave 0 planner skeleton + tolerant roadmap parser; `d711143`
  Wave 1 (weighting, bounded decomposition, v3 emission); `2f3876d`
  quota-spreader plugin (per-route envelopes, even-sparcing, recurring-
  authority reservation for K3, config-first overrides, expensive-model
  escalation); `a58fe86` Wave 2 closed-loop planner-cycle (scan, dedup,
  gate); `24cb470` signals layer + on-demand planner-cycle; `b3c5274`
  Wave 3 planner dispatch ledger + status summaries.
- Counts through the waves: 529/529 → 567/567 fast, 1752/1790/2276 full.

## Social-senses + live-orchestration design (`70a7938`→`08d8741`→`33e52d0`)

- `70a7938` social-senses: agent signal/emote layer (thought-trace,
  signals); integrated into sense taxonomy + near-term work.
- `08d8741` live-orchestration capture (observe, steer, guard-rails,
  continual param opt); `af040fb`/`cb2e7a8`/`9b66728` steering-surface
  de-risk + ACP-everywhere + LSP design + ACP transport research (CL-first
  confirmed on cxxxr/jsonrpc).
- See `docs/design/live-orchestration.md`, `agent-client-protocol.md`.

## ACP stack A1-A4 (M9.17-M9.21)

- `c9d6f5c` A1 ACP client: initialize/new/prompt/update/cancel/permission
  over stdio JSON-RPC; capability-steer negotiation.
- `b7c5635` A2 ACP dispatch driver: run a task through an ACP agent
  subprocess; fail-closed (spawn inside handler-case).
- `d6328b3` A3 steering primitive: `acp-steer-command` (scored situation →
  :none/:steer/:interrupt) + `acp-steer` on a live session; fail-closed on
  non-numeric scores.
- `95d61e2` A4 ACP server + framing fix: `hngh acp` exposes Hngh as an ACP
  agent; NEW `src/plugins/acp-transport.lisp` = JSON-RPC mode `:acp` with
  **newline-delimited framing** (ACP's wire spec — the stock cxxxr/jsonrpc
  stdio transport uses LSP Content-Length and does NOT interoperate with
  real ACP peers). INTEROP-verified driving `bin/hngh acp` with plain
  newline JSON. This was the critical correctness fix.
- 633/633 fast, 2294/2294 full.

## Cost policy + cogmem drop

- `ff61698` cost-conservation policy: >$0.10/M tokens (GLM-5.2 $0.40, K3) =
  strategic reserve; seats demoted PM/Designer to deepseek-v4-flash-0731.
- `0dc36a0` cogmem dropped (ADR-042): Anthropic-key dependency conflicts
  with cost policy; uninstalled from Hermes/OpenCode/Hngh, systemd unit
  removed; notes already in optmem.

## What's next

- L2/L3 situation-scoring design capture; research doc relocation; handoff
  (08-08).

## Decisions

- Mini-ADR: ACP must use newline-delimited framing (ACP spec), never the
  library's Content-Length stdio transport — in-repo tests pass either way
  but real peers do not (95d61e2).
- Mini-ADR: seat cost tier = >$0.10/M is reserve, so GLM-5.2 no longer a
  PM/Designer default (ff61698).
- ADR-042: drop cogmem.