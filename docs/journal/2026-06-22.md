# Journal — 2026-06-22

## Session 0B: Build System + CI Scaffolding

### What was done
- Installed SBCL 2.6.5 on the system (via pacman)
- Created ASDF system definition (`hngh.asd`) with two systems: `hngh` (core) and `hngh/tests`
- Created package definitions (`src/packages.lisp`) for all 8 core+plugin packages
- Created entry point (`src/core/main.lisp`) with `start`, `stop`, `main`, logging functions, CLI arg parsing (--version, --help, --hngh-home)
- Created custom test harness (`tests/unit/harness.lisp`) — minimal framework with `define-test`, `assert-true`, `assert-equal`, `assert-condition`
- Created Makefile with all targets: `all`, `build`, `daemon`, `run`, `test`, `repl`, `install`, `uninstall`, `clean`, `help`
- Created system daemon C skeleton (`src/system-daemon/main.c`) — connects to dbus system bus, acquires name, no methods yet
- Created system daemon Makefile
- Updated CI workflow to install SBCL, build, and test
- Created directory structure matching design spec

### What was learned
- SBCL `FORMAT` strings interpret `~` as a format directive — `~/.hngh/` in a FORMAT control string must be escaped as `~~/.hngh/` or the string breaks compilation. Only affects FORMAT control strings, not docstrings.
- Makefile phony targets can't share names with directory targets — `build` as both a phony target and the build directory caused a circular dependency. Fixed by renaming build dir to `bin/`.
- The `hngh:main` symbol must be exported from the `hngh` package for `save-lisp-and-die :toplevel` to find it.
- SBCL standalone binary is ~37MB for the current minimal skeleton (no external deps). Expected to grow as components are added.

### What's next
- Session M0.1: SBCL project skeleton — flesh out the entry point with proper initialization, logging levels, and configuration loading. The stub from 0B is the starting point.
- Session M0.2: Event bus (A2) — the first real core component.

### Decisions made
- D-005: Custom test harness instead of FiveAM — keeping dependencies minimal for M0 skeleton. Can migrate to FiveAM later if the custom harness becomes limiting.
- D-006: Build directory named `bin/` (not `build/`) — avoids Makefile phony/directory name collision.