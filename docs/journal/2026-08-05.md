# 2026-08-05

## Model mandate applied to squads (`ded6275`)

- 2026-08-05 model mandate written down and wired into hngh squad model
  chains: reserve-tier models limited, cheap/local primary. See
  `docs/journal/20260805-model-mandate.md` for the mandate detail.

## Squad test-loop optimization completed (M9.4) — `b7a0289`, `c3213dd`

- Fast-suite gate introduced (Makefile test-fast/test-suite/test-full split,
  ~15s timeout); `make test-fast` 205/205 @ 2.6s; `make test-full` 1406/1406.
- Verification exposed and fixed five real bugs in committed squad code:
  fiveam `run!` form quoting, `%git` outside squad root, `%atomic-write` name
  mangling, ANSI-CL newline literal, `nreverse` count footgun, `%find-role-dirs`
  uiop signature, lint parse.
- `be9581b` fixed the time-rotted hngh-up C3 continue-policy fixture.

## Dual-push mirror policy recorded (`a3e2889`)

- github + codeberg dual-push documented as the repo-publish policy (D-002).

## What's next

- M9 resume (mandate-to-code sync, plugin wiring, build-fence fix); W5
  prompt matrix; free-tier refresh; benchmark sourcing.