# 2026-07-31

## MC-1 tmux QoL (moonshotai/kimi-k3 via OpenRouter, Hermes TUI)

Session resumed from SESSION-HANDOFF.md (tool-channel failure restart).
Wave plan: `.omc/plans/mission-control-v2.md` (sysconfig_mgmt repo).

- `~/.tmux.conf` created: mouse on, vi mode-keys, `remain-on-exit on`,
  broadcast C-l binding (prefix C-l clears junk escape residue in all panes).
- `mc` hardened: `pane_cmd` function per pane index (single source of pane
  commands), `mc refresh` respawns dead panes (`tmux respawn-pane -k`),
  `mc status` marks `[dead]`. Verified on scratch session mc-test:
  `kill -9` on pane PID → dead flagged → refresh respawned correct command.
  Incident: `tmux kill-server` during testing killed the live hngh-mc
  session; restarted with new config, all four panes recovered.
- gbd: `~/.tmux.conf` added to agent-configs tracked_paths; commit 924641b.

## MC-3 llmtrim ↔ opencode (moonshotai/kimi-k3)

- Root cause of "opencode invisible in llmtrim": **attribution, not routing**.
  opencode traffic was proxied all along (HTTPS_PROXY→llmtrim:43117), but
  llmtrim logs under the upstream model name (`moonshotai/kimi-k3`) —
  indistinguishable from Hermes TUI traffic. Smoke: 1,568→1,595 requests.
- opencode v1.18.8: models via "opencode zen" gateway; refs need the
  `opencode/` prefix (bare/`openrouter/` refs → ProviderModelNotFoundError).
  Delegation pattern: `llmtrim wrap opencode run '<brief>' --model opencode/<m>`.

## Spend gate: llm-budget (moonshotai/kimi-k3)

- `~/.local/bin/llm-budget` (Python stdlib): rolling 60-min OpenRouter spend
  from /api/v1/credits lifetime totals; state in ~/.cache/llm-budget/. Exit
  0 ok / 1 over / 2 unknown (fail-closed with --strict). Default $10/hr
  (LLM_HOURLY_LIMIT). Local models exempt by construction (never transit
  OpenRouter). Rate marked unreliable until window ≥15min.
- Cron `llm-budget-sampler` (no_agent, every 5 min) keeps the window warm.

## MC-2 Emacs mission control wave 1 (opencode/kimi-k3, orchestrated + verified by kimi-k3)

- `emacs/hngh-mc.el` (284 lines): `hngh-mc-open` layout (status left, events
  right-top auto-revert-tail with journal rollover, llmtrim right-bottom),
  5s/30s self-canceling refresh timers, `hngh-mc-refresh`/`hngh-mc-close`/
  `hngh-mc-tmux-status`/`hngh-mc-svc-dash`. Byte-compile clean (Emacs 30.2),
  batch smoke MC2_OK. eat is NOT installed (recon error in wave file) — the
  eat branch compiles but is runtime-untested; compilation fallback works.
- `emacs/README.md` with load + command docs.
- Wave file: `sessions/2026-07-31-mc2-wave1.md`.
- Committed 6185477, pushed github + codeberg.

## DOC wave (moonshotai/kimi-k3)

- `docs/project/next.md` rewritten: M0–M6.2 status, 892 tests green
  (verified `make test` 2026-07-31), up-next table (M7, MC-2, M8, svc-dash
  wave 6 + PyPI, gbd TUI). Was stale since 2026-06-24 ("M1 batch 5").
- `docs/project/backlog.md`: date stamp + M2 reality note. Committed 6185477.

## NET-1 notes (moonshotai/kimi-k3)

- `docs/project/roadmap.md` M3 section: NET-1 prep notes — M7 protocol should
  be auth/remote-ready; Steam Deck first LAN node (needs M1.14); Steam
  non-game distribution via systemd --user; device pooling + social peering
  open questions. Commit c664bb2, pushed.

## MC-2 wave 2 (in progress)

- WP-A (agent-queue panel for hngh-mc.el): wave file
  `sessions/2026-07-31-mc2-wave2a.md`, delegated to opencode/kimi-k3.
- WP-B (emacs-daemon lifecycle plugin, M6.3): wave file
  `sessions/2026-07-31-mc2-wave2b.md`, queued for delegation after WP-A.
- `emc` launcher (~/.local/bin): daemon start/stop/status + Konsole-tab
  emacsclient frames. shellcheck clean; live-verified: daemon up (pid 439063),
  frame opens. gbd tracks it (agent-configs).
