# Journal — 2026-09-08

## 00:30Z overnight-continuity wake (plan 2026-09-02-overnight-continuity, executed six days after acceptance — routed stubs and operator-items wakes held the queue first)

- Gates: kernel `make test` green — 2855 checks (00:36Z); hngh-automation
  `make test` green rc=0 (00:35Z); kernel gate re-run green immediately
  before each ceremony.
- Step 2 (grow, sync-not-redo): both routed stubs were already executed
  by earlier wakes. slow-unit dropin:20-workbeat.sh — false-positive
  probe (bimodal workbeat by design), fixed hngh-automation 7caff48
  (jobs/slow-units.py envelope + 7 hermetic tests), ticked 2026-09-01.
  ui-audit name-completeness — two-clock race (schedule feed vs
  rendered page), fixed f9723c3 + fixture 6b9ca00 by the 00:01Z wake;
  all three identity-duplicate plans ticked in ceremony 9826af5;
  16 consecutive hourly audits clean since.
- Step 3 (research): docs/research/2026-09-08-ebook-book-inputs.md
  authored via a delegated bounded session. Material delta recorded:
  `--chapters` selection already landed in scripts/generate-publication
  since the 2026-08-30 grounding doc. Priced decision: per-book
  metadata input first (smallest fully-missing input).
- Step 4 (grow, batched ceremony): commit da3d441 — candidate
  a1021dfb, manifest 5 files (research doc; plan ticks 1-4;
  reports.md + ui-grades.md + current-overlay.json stragglers), 10/10
  principles, push to origin driven in-ceremony. Public-content
  pre-flight fixed two historical reports.md rows that tripped the
  gate: line 987 credential-shaped (an unsloth-token probe alert
  whose colon-plus-word text matched the credential regex; reworded
  with an em dash) and line 1431 absolute home path (re-rendered
  in ~/-form as cwd ~/Projects/etc/llm-wiki). Rows are
  display records; the body files keep the original text.
- Step 5 (wrap): lessons-2026-09-02.md appended (dated 2026-09-08);
  this journal; queue row 42 + backlog ebook-book-inputs entry synced
  to cite the research doc; next plan authored at
  docs/project/plans/2026-09-09-overnight-continuity.plan.md
  (status=proposed) — the plan-supply law holds. Wrap ceremony landed
  the same wake.
- Excluded from all manifests: docs/research/
  2026-09-08-ctx-retrieval-vs-repetition.md — the artifact carries
  transcript corruption from its research-beat session (stray model
  narration + raw tool-call blocks). Left untracked for the owning
  line to rewrite; not landed because it exists.
- A concurrent lane (automation-subtree-import) was live in the
  kernel tree mid-wake (systemd unit edits appearing in the working
  tree); explicit-file ceremony manifests kept its in-flight files
  untouched.
