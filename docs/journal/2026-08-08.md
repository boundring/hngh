# 2026-08-08

## L2/L3 situation-scoring design capture (M9.22) — `b01a0ac`

- Full design of the auto-steering "brain" behind the A3 ACP actuator
  (`docs/design/situation-scoring.md`): L2 recognition + L3 scoring, with
  three grounded evidence bases —
  1. **/steer case-base**: mined 243 real human `/steer` messages from
     `~/.hermes/.hermes_history`; dominant classes = wasted waits, faulty
     logic, not-sourcing-info, risky-experiment, coordination, cost/token,
     stuck-seat.
  2. **Failure-modes research** (subagent): 23-source reference on
     self-correction + hallucination (Huang, Kamoi, MINT, ReAct, MAST,
     tau-bench).
  3. **Prior-art design space** (subagent): 30+ source borrow-list
     (guardrails, OTel GenAI, PRM/CriticGPT, OpenFang loop-guard, LangGraph
     HITL) + 6 explicit gaps.
- Key decisions: recovery-STAGE scoring (not single faults) — escalate only
  on 2× unvalidated same-class / S3 / long-thinking-zero-env; weight the
  ACTING stream over THINKING; Tier-0 procedural detectors first; cheap/
  local calibrated judge; progressive gate-lowering log→steer→rewrite→ask→
  interrupt; open taxonomy + self-improvement loop (case-base + review +
  web re-grounding).
- `aa2e88e` moved both research reference docs into `docs/research/`.

## Handoff (M9-end)

- `42e834d` refreshed `docs/project/next.md` as the continuation point:
  full handoff brief (A1-A4, framing fix, L2/L3 design, cost policy, cogmem
  drop), immediate next work (L2/L3 build steps 1-6), Up Next repointed.
- Task 91 (A3+A4) archived to `.done/`; task 92 (L2/L3 build) staged at
  `~/.hngh-night/tasks/` with waves + acceptance criteria.

## Misc

- MisakaNet nodes registered: Hermes = Misaka10062 (#870), opencode =
  Misaka10064 (#900, resubmitted after #871 was closed `completed` before
  the workflow ran; the bot's guidance was to open a new issue). Both entry
  tests (pip/SSL search) completed.
- Journal-gap backfill (this file set): 08-01 → 08-08 entries written to
  restore journal continuity.

## What's next

- L2/L3 build steps 1-3 (Tier-0 detectors, scoring, gate-lowering → A3)
  per `situation-scoring.md` §8 / task 92; then steps 4-6.

## Squad seat startup fix (opencode model-string naming)

- Diagnosed "opencode members don't start": coder/worker seats set
  `SEAT_MODEL=deepseek/deepseek-v4-flash-0731`. opencode DOES recognize
  `deepseek` as a provider, but only offers simply-named models there
  (`deepseek/deepseek-v4-flash`, `deepseek/deepseek-v4-pro`); `deepseek-v4-
  flash-0731` (`-0731`) is **openrouter's catalog name**, not a deepseek-
  provider name. Since `deepseek-v4-flash` == `deepseek-v4-flash-0731` (deepseek
  serves that as its latest under a simpler name), the bad string never
  resolved and every opencode kick died with `UnknownError: Unexpected server
  error` — the window opened (squad-up reported RUNNING) but the first prompt
  failed on model resolution.
- Two valid routes: `deepseek/deepseek-v4-flash` (deepseek direct API) or
  `openrouter/deepseek/deepseek-v4-flash-0731` (same model via openrouter).
  We use **Flash** because it's the smarter model for now, and default to the
  **openrouter** route because it's usually cheaper — a cost preference, not
  a rule; when deepseek-direct prices lower, it's the valid alternate.
- Auth was fine: `OPENROUTER_API_KEY`/`DEEPSEEK_API_KEY` are exported in
  `~/.hermes/.env` (lines 507/496, sourced by the interactive shell; not in
  the Hermes terminal env — located by name, never printed).
- Verified: `set -a; . ~/.hermes/.env; set +a; opencode run -m
  openrouter/deepseek/deepseek-v4-flash-0731` → `OPENCODE_SMOKE_OK`.
- Fix: coder/worker `SEAT_MODEL` → `openrouter/deepseek/deepseek-v4-flash-0731`
  in `~/.hngh-night/squad-seats.conf` (with corrected NOTE comments). Hermes
  CLI seats correctly keep `deepseek/...` (Hermes maps it). `squad-up --list`
  parses clean. Pitfall #27 in the multi-agent-coordination skill corrected to
  name the real cause (deepseek provider exists; `-0731` is an openrouter
  catalog name, not a deepseek-provider model).

## Squad startup → ACP integration (design direction, not built)

- User direction: squad startup should integrate ACP calls (kick seats over
  `session/prompt` with a text content block — the durable fix that eliminates
  the paste-race, per situation pitfall #23), while KEEPING the cascading
  Konsole windows so each seat's session is visually observable. This pairs
  the structured ACP kick (reliable, no clipboard/image ambiguity) with the
  per-seat observation surface the windows already provide. Span: the coder/
  worker seats are opencode seats driven by ACP; Hermes-seat equivalents via
  its ACP server. Recorded here as the direction; build is a later wave
  (see agent-client-protocol.md / squad-autonomy.md).