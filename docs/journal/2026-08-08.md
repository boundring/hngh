# 2026-08-08

## L2/L3 situation-scoring design capture (M9.22) — `b01a0ac`

- Full design of the auto-steering "brain" behind the A3 ACP actuator
  (`docs/design/situation-scoring.md`): L2 recognition + L3 scoring, with
  three grounded evidence bases —
  1. **/steer case-base**: mined 243 real human `/steer` messages from
     `~/.hermes/.hermes_history`; dominant classes = wasted waits, faulty
     logic, not-sourcing-info, risky-experiment, coordination, cost/token,
     stuck-seat.
  2. **Failure-modes research** (subagent): 23-source reference on
     self-correction + hallucination (Huang, Kamoi, MINT, ReAct, MAST,
     tau-bench).
  3. **Prior-art design space** (subagent): 30+ source borrow-list
     (guardrails, OTel GenAI, PRM/CriticGPT, OpenFang loop-guard, LangGraph
     HITL) + 6 explicit gaps.
- Key decisions: recovery-STAGE scoring (not single faults) — escalate only
  on 2× unvalidated same-class / S3 / long-thinking-zero-env; weight the
  ACTING stream over THINKING; Tier-0 procedural detectors first; cheap/
  local calibrated judge; progressive gate-lowering log→steer→rewrite→ask→
  interrupt; open taxonomy + self-improvement loop (case-base + review +
  web re-grounding).
- `aa2e88e` moved both research reference docs into `docs/research/`.

## Handoff (M9-end)

- `42e834d` refreshed `docs/project/next.md` as the continuation point:
  full handoff brief (A1-A4, framing fix, L2/L3 design, cost policy, cogmem
  drop), immediate next work (L2/L3 build steps 1-6), Up Next repointed.
- Task 91 (A3+A4) archived to `.done/`; task 92 (L2/L3 build) staged at
  `~/.hngh-night/tasks/` with waves + acceptance criteria.

## Misc

- MisakaNet nodes registered: Hermes = Misaka10062 (#870), opencode =
  Misaka10064 (#900, resubmitted after #871 was closed `completed` before
  the workflow ran; the bot's guidance was to open a new issue). Both entry
  tests (pip/SSL search) completed.
- Journal-gap backfill (this file set): 08-01 → 08-08 entries written to
  restore journal continuity.

## What's next

- L2/L3 build steps 1-3 (Tier-0 detectors, scoring, gate-lowering → A3)
  per `situation-scoring.md` §8 / task 92; then steps 4-6.