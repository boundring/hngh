# 2026-08-01

## H-A2/H-B1/H-B2/H-A3 — daemon lifecycle + maintenance control (hngh)

- `829f4db` H-A2 pure eligibility selector + H-B2 maintenance-state
  (staging local work behind a maintenance gate).
- `81f0da4` H-B1 read-only maintenance coordinator plugin.
- `d33a861` H-A3 pause/resume + stale-lease recovery; `e170b73` test
  parenthesis + plist fixes (tests 1xxx green after).
- `8a00a74` H-U1 user daemon lifecycle fixes.
- Phase 2 groundwork: claim/release semantics being refined (design in
  `docs/design/phase2-claim-release.md`).

## M-sentry — procedural safeguards Tier-0 (`0030153`)

- `sentry.lisp` plugin: `scan-secrets` (9 secret patterns, redacted evidence
  via `threat.flag` publish) + `context-watch` (hermes agent.log tail,
  green/yellow/red vs 256k ceiling). Procedural, fail-closed, no model in the
  hot path — llmtrim's shape. 12 fixture-based tests; 920/920 full.

## Queue v3 + cost routing v2

- `bb20683` version+validate task records (queue v3).
- `405b48b` cost routing v2 — verified faucet ladder (kimi-sub, copilot…)
  into routing; DOCS-only commit recording the ladder.

## MC-2 wave 3 (overnight) — `65012de`

- night-ralph local-loop log panel for the emacs dashboard.

## What's next

- M7 client-server daemon (wire protocol, daemon core, client CLI) — merged
  next day; Phase 2 lease expiry + completion transitions.

## Decisions

- Mini-ADR: maintenance-state (H-B2) gates local work from dispatch; H-B1
  read-only coordinator is the first consumer.