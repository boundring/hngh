# fail-20260914-system-low-mem — transient spike disposition

Status: dispositioned 2026-09-14 by m-loop respawn session (small verified step).

## Alert

- Identity: `system-low-mem` (oversight tick, flapping suppression 86400s).
- First fire: 2026-09-14T02:00:16Z alert 6f09ced8, "[oversight] system-low-mem:
  critical resource flag set"; routed to plan candidate
  `2026-09-14-routed-system-low-mem` at 02:00:20Z; dedup-suppressed ×2 at
  03:00:39Z (candidate still live).

## Mechanism (verified against source)

`automation/jobs/system-awareness.sh` samples memory percent and sets
`low_mem=true` when `mem >= LOW_MEM_PCT` (default 90, line 30/87).
`automation/jobs/oversight-tick.sh` converts the flag to the alert identity
`system-low-mem` under the same-key flapping suppression. The flag is a
point-in-time threshold, not a persistence check.

## Evidence

- The flag fired once at 02:00Z (02:00 and 03:00 rows; the 03:00/04:00 router
  rows are dedup suppressions, not new fires).
- `automation/dashboard/system.json` at 2026-09-14T18:55:00Z: mem 45.2%,
  headroom `low-mem: false`, 17 hours after the alert.
- Live machine state at disposition time: 30Gi total, 13Gi used, 16Gi
  available; top RSS consumer `unsloth` at ~5.3Gi — a nightly local-model
  workload consistent with a transient early-morning spike, matching the
  system-awareness header note that local-model endpoint behavior is "normal
  nightly behavior".
- No code defect is implicated: the threshold logic behaves exactly as
  documented, and the machine is healthy now.

## Disposition

killed — transient early-morning memory spike past the 90% threshold on
2026-09-13/14 nightly workload (unsloth local-model lane); the flag cleared on
its own, held `low-mem: false` for 17+ hours since, and there is no defect to
fix. The routed plan `2026-09-14-routed-system-low-mem` should be marked
resolved by its step 1 (subject recorded with disposition). Re-alert only if
the flag becomes persistent (multiple unsuppressed fires across days), at
which point threshold calibration, not this line, is the subject.
