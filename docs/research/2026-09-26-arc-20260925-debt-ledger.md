# debt: where do the ~19 ponytail ceiling markers index into a ledger an audit beat can consume?

Status: crystallized 2026-09-26 from research line `arc-20260925-debt-ledger`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-arc-20260925-debt-ledger.md.

# research beat — line crystallization (final record)

_line: debt: where do the ~19 ponytail ceiling markers index into a ledger an audit beat can consume? | state: contracting -> closed | record: final | verification: kernel repo NOT read this transition; vault note bodies not read — wiki index entries only_

## Verification limits (final, and binding on everything below)

This record inherits the same bound as the 2026-09-26 contraction beat: **no transition in this line's surviving material has read `[redacted path] including this one. Therefore no file paths inside the kernel are cited anywhere in this record, and nothing here should be read as a claim about kernel file contents. What can be named with confidence: the kernel repo root path itself (operator-supplied), `research-lines.tsv` (the line-state file), the five vault index entries reproduced in the line material, and the text of the prior beat. Any path that is a *proposal* rather than an existing artifact is labeled as such. One terminology note carried into the record: "ponytail" appears in the line title as received; no surviving material defines its referent. That is logged as a thread (T4), not papered over.

## Findings

**F1. The central quantity of this line was never made exact.** "~19" is an approximation that survived the line's entire lifetime unconverted. No beat with kernel read access ever ran, so the count has no provenance beyond the line title itself. The registry deliverable (R1 below) exists precisely to convert this approximation into a machine-checkable number — and to record whether the exact count confirms or ref

[truncated at model call: completion hit the max_tokens cap (finish_reason=length) - re-run the beat]
