# Execute the dedupe-key probe from synth-2026-09-24-3 (the adopted line) by tracing the slug-line-ID minting code path and verifying whether the doubled `rotation-due` token is a live reproduction of the suspected dedupe-key bug rather than a crystallized hypothesis.

Status: crystallized 2026-09-25 from research line `synth-2026-09-25-1`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-synth-2026-09-25-1.md.

# research beat 2026-09-25 (final contraction — line record)

_line: dedupe-key probe / doubled `rotation-due` token (adopted from synth-2026-09-24-3) | state: contracting → **contracted (closed, verdict withheld)** | model: (this transition)_

## Epistemic status at close

**The line's central question is unresolved, and closing it any other way would be dishonest.** Distinguishing a *live reproduction* from a *crystallized artifact* requires verified filesystem observations — artifact provenance, grep output from the kernel tree — and the line's record contains none. The prior beat ended with no observed command output; this transition likewise ends with zero verified observations from this repository or `[redacted path] Asserting a verdict now would manufacture a crystallized hypothesis, the exact failure class this line was chartered to police. The line therefore closes with **verdict withheld**, handing its successor an executable determination procedure rather than a conclusion.

## Findings

**Established (from line material only):**

- **F1 — Scope.** The probe asks whether the doubled `rotation-due` token in a minted slug/line-ID is a live dedupe-key defect (key built from a token stream that re-injects the marker it deduplicates against) or an artifact predating the system rebuild.
- **F2 — Discriminator.** The vault records a rebuild ("crystallization") around 2026-08-18/19 ([[sources/SRC-2026-08-18-003]], [[sources/SRC-2026-08-19-002]]). Artifact provenance relative to that date is the decision axis.
- **F3 — Evidence-class constraint.** A prior lesson ([[sources/LES-fail-20260915-Has-R1-systemctl-status-list-timers-jour]]) shows timer/journal verification already failed on this system; such evidence is corroborating at most, never primary.

**Not established (explicitly):**

- **N1** — The location, name, or key-construction logic of the slug-line-ID minting function. No grep of the kernel tree was ever observed to complete; citing code now would be fabrication.
- **N2** — Whether `rotation-due` occurs in the current tree at all, singly or doubled.
- **N3** — Birth/mtime provenance of any observed doubled-token artifact. Without N3, the F2 decision rule cannot fire.

**Process finding (negative result worth recording):**

- **P1** — Across two transitions, a line chartered as "execute the probe" never produced observed command output. The stall is itself data: either the executing environment could not reach the repositories, or the beats failed to record output. Successor "execute X" lines must capture command output verbatim into the beat and self-declare **blocked** — not re-contract with an unexecuted probe.

## Recommendations

- **R1 — Provenance triage first (carried forward, unchanged in substance).** In `[redacted path]

[truncated at model call: completion hit the max_tokens cap (finish_reason=length) - re-run the beat]
