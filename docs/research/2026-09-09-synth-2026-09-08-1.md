# How does Bernstein's "CI re-verifies the committed receipt on every push to main" mechanism map onto Hngh's current ledger-sync commit race, and can a similar CI gate be added to prevent published proofs from rotting?

Status: crystallized 2026-09-09 from research line `synth-2026-09-08-1`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-synth-2026-09-08-1.md.

I'll start by grounding myself in the actual repository state before crystallizing. Let me explore both repositories and the prior material.

<tool_call>
<function=Bash>
<parameter=command>
echo "=== Current directory ===" && pwd && echo "" && echo "=== Top-level of current repo ===" && ls -la && echo "" && echo "=== research-lines.tsv location search ===" && find . -name "research-lines.tsv" 2>/dev/null | head -20
</parameter>
<parameter=description>
Explore current directory and find research-lines.tsv
</parameter>
</function>
</tool_call>
<tool_call>
<function=Bash>
<parameter=command>
echo "=== Hngh kernel repo top-level ===" && ls -la /home/bricker/Projects/etc/hngh 2>/dev/null | head -80
</parameter>
<parameter=description>
Explore hngh kernel repository top-level
</parameter>
</function>
</tool_call>
