# patrol-20260923-rotation-due-rotation-due — what is the evidence for the broken dedupe key and demotion reachability assumptions that currently block OT1 and OT2 progress?

Status: crystallized 2026-09-24 from research line `synth-2026-09-24-3`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-synth-2026-09-24-3.md.

# Crystallization — patrol-20260923-rotation-due-rotation-due

**Line question:** What is the evidence for the broken dedupe key and demotion reachability assumptions that currently block OT1 and OT2 progress?
**Lifecycle:** expanding → **contracting (final record)**
**Recorded:** 2026-09-24

---

## Honest status statement (read first)

This line is being crystallized with **no verified evidence in hand**. The prior beat's material consists entirely of *search plans* — grep/find command sketches with `[redacted path]` placeholders and **no captured output**. No file contents from this repository or from `[redacted path] were actually read, quoted, or confirmed to exist during the line's lifetime. I therefore cannot assert that any dedupe key is broken or that demotion reachability is misimplemented; I can only record what was assumed and what remains unverified. Rather than fabricate findings to make the contracting state look complete, this record documents the evidentiary gap itself as the line's lasting result.

## Findings

1. **The blocking assumptions were never evidenced.** The line premise asserts two things: (a) a dedupe key is "broken," and (b) demotion is unreachable in a way that blocks OT1/OT2. Neither claim is supported by any quoted code, log line, test failure, or commit reference in the accumulated material. Both remain **unverified hypotheses**.
2. **The search strategy was sound but never executed to ground.** The prior beat enumerated the right probe classes: dedupe-key construction (`dedupe_key`, hashing via `hashlib`/`sha256`), demotion logic (`demote`, `demotion`, `reachability`), queue/priority interaction, configuration schemas, TODO/FIXME markers, and OT1/OT2 blocking logic. This plan is the line's reusable artifact.
3. **Prior art provides framing, not evidence.** Vault pointers [[sources/outcome-demotion-at-two-consecutive-failures]] and [[sources/mid-line-verification-block-triggers-long-acceptance-pending]] suggest the broader system already encodes demotion-trigger semantics (demotion after consecutive failures; verification blocks triggering long acceptance-pending states). These are conceptually adjacent to the "demotion reachability" question but say nothing about this repository's implementation.
4. **No confirmed file paths exist for either repository.** Per the grounding requirement, I decline to cite any path inside the hngh kernel tree, since none was actually observed. The only artifacts I can name with confidence are `research-lines.tsv` (line state, per this prompt) and the vault pointers supplied as prior art.

## Recommendations

1. **Re-open with execution, not planning.** A successor line should run the prior beat's probes *with real paths and captured output*, then quote the actual dedupe-key construction site before claiming it is broken.
2. **Test the dedupe-key hypothesis directly.** Concretely: locate the key derivation, feed it the inputs implicated in the "rotation-due-rotation-due" pattern (the doubled `rotation-due` in this line's own slug is suggestive of a key-composition bug worth checking), and record whether distinct inputs collide or identical inputs diverge.
3. **Test demotion reachability as a state-machine question.** Enumerate the states/queue levels an outcome can occupy, then check whether any transition path leads into the demote branch — the "consecutive failures" semantics in the prior art suggest a counter-based trigger whose counter may never increment or never be read.
4. **Resolve OT1/OT2 dependency explicitly.** Whatever OT1 and OT2 are in the tracker, the claim that they are *blocked by* these two assumptions should be written down with the blocking mechanism stated, so a future line can confirm or refute it rather than re-derive it.

## Open threads

- **OT-A:** Actual dedupe-key construction site and its collision behavior under rotation-dedup inputs — unexamined.
- **OT-B:** Reachability analysis of the demotion branch (counter increment/read path) — unexamined.
- **OT-C:** The doubled `rotation-due` token in this line's own identifier — possibly a live instance of the suspected key bug; worth treating as a specimen.
- **OT-D:** Verification that `[redacted path] contains the assumed demotion/rotation modules at all.

## References

- `research-lines.tsv` — line state (named in the line prompt; path relative to this repository).
- `[[sources/outcome-demotion-at-two-consecutive-failures]]` — llm-wiki vault pointer (read-only; supplied as prior art).
- `[[sources/mid-line-verification-block-triggers-long-acceptance-pending]]` — llm-wiki vault pointer (supplied as prior art).
- `[[concepts/evidence-ledger]]` — llm-wiki vault pointer (framing only).
- **Not referenced:** any path within `[redacted path] — none was verified to exist during this line; citing one would be fabrication.

*Crystallization verdict: line closes as an **evidence gap record**, not a confirmed bug report. The assumptions blocking OT1/OT2 remain assumptions.*
