# Ground-truth pass on synth-2026-09-20-1/2/3

Status: crystallized 2026-09-21 (machine session, direct code reads; each
claim cites the exact file:line it was read from). These three planned
research lines asked for kernel/automation ground truth that was available
in-repo, so the pass was executed inline instead of queued as model beats.

## synth-2026-09-20-1 — byte-level serialization invariants

The source line (crystallized 2026-09-20 as
`docs/research/2026-09-20-fail-20260920-What-is-the-exact-byte-level-serializati.md`)
carried only unverified open threads. Verified now:

- **F3 resolved (language):** kernel is Common Lisp on SBCL; records are
  s-expression lines, not JSON. The doc's six-property JSON framing was
  built on the unknown-language premise and partially dissolves.
- **R1 deliverable (writer located):** the record/audit writer is
  `src/main.lisp:63` `default-record-callback` →
  `src/adapter/filesystem.lisp:118` `store-record-run`, which appends
  one form per line through the `with-open-file` at
  `src/adapter/filesystem.lisp:110` (hardened 2026-09-21 by commit
  142a1899: read-side `read-lines` converts unreadable state to
  TRANSPORT-FAULT).
- **UTC normalization at the writer boundary: verified TRUE.**
  `src/main.lisp:55-58` `format-utc-timestamp` decodes with timezone 0 and
  emits fixed-width `YYYY-MM-DDTHH:MM:SSZ`; the single clock callback
  (`src/main.lisp:63-65` `default-clock-callback`) is the only emitter, and
  `src/adapter/federation.lisp:399-406` parses that exact shape back.
  `src/domain/attestation.lisp:161` pins the emitted shape.
- **R4 (golden-file coverage) already held:** the round-trip is pinned by
  the kernel suite (`tests/adapter/test-filesystem.lisp`; gate 2934 checks
  green 2026-09-21, commit 142a1899).

Residual: none blocking. A written R2-style spec doc would be nice-to-have
but the invariants it would name are already enforced and tested.

## synth-2026-09-20-2 — journal-error state persistence

The line's premise ("an error unit was observed but not durably persisted;
next patrol treats the lane as fresh") does not match the mechanism:

- `unit-not-practiced` is a **deliberate fail-closed verdict**, not a
  lost-memory defect: `automation/jobs/patrol.py:1434-1437` — a failed
  unit whose name is not in the restart allowlist gets
  `fails.append((sig, "unit-not-practiced", ...))` with explicit
  "no auto-action".
- Persistence **exists**: `automation/jobs/patrol.py:1439/:1462`
  (`journal_counts_load` / `journal_counts_save`) persist per-(sig, unit)
  restart counts keyed by date.
- Auto-closure expectations already present: the restart-guard daily cap
  (`patrol.py:1441-1444`) and the docstring convention that a repeated
  unclaimed pair auto-queues research (`patrol.py:1399-1406`).

Disposition: premise refuted; the consecutive-failure escalation the line
worried about is the designed fail-closed path, not a persistence hole.

## synth-2026-09-20-3 — verify the two load-bearing facts of synth-2026-09-19-2

Both facts verified in code, and both point the same direction: the
recommended hardening already exists.

- **Staging scope (R1):** `git add -- docs/project/plans/` — pathspec-limited, not
  `git add -A` — in `automation/cadence/day/14-plan-ledger-sync.sh:26`.
- **Dirty-lane guard (R3):** the same script refuses entirely when the
  kernel repo has staged changes (`14-plan-ledger-sync.sh:21-24`).
- **Path segregation (R2):** holds — the sync touches only
  `docs/project/plans/`.
- **Persistence failure mode (F1):** nonexistent as claimed — see
  synth-2026-09-20-2 above.

The config-backup `git add -A` site (`automation/jobs/config-backup.sh:16`)
is the dotfile-backup lane (own snapshot repo, secret-scanned, fail-closed),
not the kernel ledger sync — different repo, out of scope for the
ledger-sync race.

## Verification

- All claims read directly from the cited files on 2026-09-21.
- Gate: `make test` 2934 checks green (2026-09-21, commit 142a1899).
