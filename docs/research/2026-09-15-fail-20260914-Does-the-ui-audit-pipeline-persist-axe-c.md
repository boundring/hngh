# Does the ui-audit pipeline persist axe-core JSON output to an `artifacts/` directory, and if so, what is the CSS selector in `nodes[0].target` for the `aria-required-children` violation on `#tabs`?

Status: crystallized 2026-09-15 from research line `fail-20260914-Does-the-ui-audit-pipeline-persist-axe-c`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-fail-20260914-Does-the-ui-audit-pipeline-persist-axe-c.md.

I'll resolve this line by actually inspecting both repositories now, rather than re-asserting the prior beat's hypotheses. Starting with orientation.
