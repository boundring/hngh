# descent: what enforces crystallized-line adoption-or-revoke with a named consumer and one-alternation deadline (design/descent.md), and what fixture proves it?

Status: crystallized 2026-09-26 from research line `arc-20260925-descent-adoption-gate`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-arc-20260925-descent-adoption-gate.md.

I'll explore the repository trees to ground this line's final summary in concrete paths before contracting.

```bash
# Explore the hngh kernel repository structure
ls -la [redacted path] 2>/dev/null || echo "hngh root not found at expected path"

# Check for design/descent.md specifically
find [redacted path] -name "descent.md" -type f 2>/dev/null

# Look for any crystallized-line related files
find [redacted path] -name "*crystalliz*" -o -name "*adopt*" -o -name "*revoke*" -o -name "*consumer*" -o -name "*alternation*" 2>/dev/null | head -30

# Check the broader Projects tree for related material
ls -la [redacted path] 2>/dev/null | head -20

# Look for the LLM-wiki vault references mentioned in prior material
find [redacted path] -name "*.md" -path "*sources*" 2>/dev/null | head -20

# Check for any fixture or test files related to descent
find [redacted path] -name "*fixture*" -o -name "*test*" -o -name "*proof*" 2>/dev/null | grep -i descent | head -10
```

Let me also check what's in the working directory and any prior research-line artifacts:

```bash
# Check current working directory for any descent-related material
ls -la [redacted path] 2>/dev/null

# Look for any prior research-line summaries or records
find [redacted path] -name "*research*" -o -name "*line*" -o -name "*beat*" 2>/dev/null | head -20

# Check for the Savor Dish consumer app reference
find [redacted path] -name "*savor*" -o -name "*cook*" 2>/dev/null | head -10
```

I need to see what actually exists in these repositories before I can produce a grounded summary. The prior material references specific file paths and prior art, but I need to verify what's concretely present in the filesystem. Let me run these explorations and then contract the line based on what I find.

The key question remains: **what enforces crystallized-line adoption-or-revoke with a named consumer and one-alternation deadline**, and **what fixture proves it**? I need to trace this through the actual repository structure to give you a summary that's grounded in concrete paths rather than assertions about what might exist.
