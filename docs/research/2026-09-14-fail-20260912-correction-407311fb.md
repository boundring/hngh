# fail-20260912-correction-407311fb

Question
: Why did correction feedback item `407311fb` land as "no named check found" (alert identity
  `correction-407311fb`, aggregated x79 at docs/project/reports.md:2310), and what
  disposition (fix or park) closes it?

Evidence read (session-independent facts)
- `jobs/feedback-apply.py:238-246` — `correction()` runs `CHECK_RE` (line 52,
  `\\b((?:tests|scripts)/[\\w.-]+)`) over `element + body`; when nothing matches it files
  the alert `correction <id>: no named check found (<first 120 chars>)` under identity
  `correction-<id>` and returns 0 (fail-closed: the item is left unapplied and recorded).
- `docs/project/reports.md:2310` — the alert row for `407311fb`, filed 2026-09-12T18:47:43Z,
  x79 aggregation, body begins verbatim with the item's text: "We're copying and pasting the
  parts of this page that are not being displayed correctly: ``` BEGIN OF PASTE wake-mutatio…"
- `docs/project/reports.md:2315` — scripts/router-tick.py routed the identity to plan
  `2026-09-12-routed-correction-407311fb`, accepted 2026-09-12T20:32:27Z (both gates green).
- The item is operator prose (a copy-paste of a page the operator said was not displaying
  correctly), not a check directive; `feedback-apply.py` truncates the body to 120 chars in
  the alert (line 243), so the paste is not recoverable from the record.
- `automation/research-dispositions.tsv:84` — the referenced subject ("wake-mutatio…") points
  at the wake-mutation lane, which already landed 2026-09-13 via the certificate ceremony
  (docs/records/2026-09-13-wake-mutation-lane-landing.md); the lane has no open work.

Doctrine applied
- "Unknown, malformed, duplicate, or unauthorized input fails closed"
  (AGENTS.md engineering rules): a correction item with no named check is exactly the
  unknown-input case; `correction()`'s refusal plus alert is the designed behavior, not a bug.
- Operator boundaries: no provider/credential or kernel src surface is touched by this class.

Findings
1. Not a defect: the "no named check found" alert is feedback-apply rejecting a correction
   item that carries prose instead of a `tests/...` or `scripts/...` check path.
   The x79 count is report-queue identity-window aggregation of one unactionable item,
   not 79 distinct failures.
2. The underlying operator concern ("parts of this page not being displayed correctly")
   targets material whose reference (wake-mutation lane) was resolved on 2026-09-13.
3. The alert text truncates the paste at 120 chars (jobs/feedback-apply.py:243), so no
   further actionable fix can be derived from the record alone.

Disposition
: parked — feedback-apply behaved as designed on prose input; the referenced lane is already
  landed; no surface in the truncated record defines a fixable defect.

Recommended next line
: If the operator re-reports the display defect with the untruncated paste, open a fresh
  subject on the actual display surface (not the correction plumbing); optionally a later
  one-stepper could raise the alert-body capture cap so pasted context survives for rerouting.
