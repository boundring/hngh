# Why did feedback-apply correction item 407311fb land as "no named check found" (alert identity correction-407311fb, x79 at reports.md:2310) for an operator prose paste ("we are copying and pasting the parts of this page that are not being displayed correctly" -> wake-mutatio...), and what disposition (fix or park) closes it?

Status: crystallized 2026-09-14 from research line `fail-20260912-correction-407311fb`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-fail-20260912-correction-407311fb.md.

# Crystallized Line: Correction 407311fb — "no named check found" Disposition

**Line:** Why did feedback-apply correction item `407311fb` land as `no named check found` (alert identity `correction-407311fb`, x79 at `reports.md:2310`) for an operator prose paste, and what disposition (fix or park) closes it?
**Lifecycle:** contracting → **crystallized (lasting record)**
**Epistemic status of this record:** derived from the supplied alert metadata and the prior expanding/contracting beats. I have no file-read access in this transition; nothing below is grounded in a file I opened. See *Verification boundary* and *References*.

---

## 1. Verification boundary (binding)

Three tiers, kept distinct so the record cannot be misread as confirmed fact:

- **Supplied (alert metadata / line description):** item id `407311fb`; alert identity `correction-407311fb`; terminal string `no named check found`; occurrence count x79; pointer `reports.md:2310`; operator prose ("we are copying and pasting the parts of this page that are not being displayed correctly"); ingestion channel wake/mutation.
- **Inferred (analysis, not directly observable here):** that the terminal state reflects a *coverage gap* rather than a one-off matcher fault; that x79 denotes steady-state accumulation; the specific root-cause mechanism.
- **Unverifiable in this transition:** the actual bytes at `reports.md:2310`; any structured field on the stored record (e.g. a `registry_version_mismatch` / sync flag vs. a plain null/no-match identity); matcher internals under `/home/bricker/Projects/etc/hngh`.

The inference tier is the *most probable* reading of the supplied metadata, not an established fact. The single most important open action is to read the real line before any fix ships (see Open threads).

## 2. Findings

**Root cause (inferred, carried from prior beat).** `407311fb` is a prose *workaround description* — not a structured defect report — that entered through the wake/mutation ingestion channel. The feedback-apply matcher found no registered named check whose scope overlaps the text, so it terminated as `no named check found`. There is no check identity for an operator rendering-workaround anecdote to land on.

**x79 = steady state of a coverage gap, not a one-off.** A count of 79 identical terminal states indicates the prose-report class has *repeatedly* reached the matcher with nothing to match against. This is the recurring cost signature: the pipeline keeps re-deriving the same dead end for the same input shape.

**Alternative hypothesis (must be ruled out before fixing).** If the stored record carries a structured field such as `registry_version_mismatch` rather than a plain unmatched identity, the cause shifts from *coverage gap* to *registry sync failure*. The supplied metadata does not disambiguate; only reading the actual line does.

## 3. Disposition — **park the instance, fix the class**

**Park `407311fb` itself.**
1. The report describes a transient, host-specific rendering fault with no stable oracle — "did this page render correctly on that host at that moment" is not recheckable after the fact. This is precisely the freshness attestation absent here (cf. [[concepts/moment-of-action-freshness]]).
2. The operator's own workaround (copy-paste) confirms the content was ultimately retrievable; there is no outstanding correctness debt on this instance.
3. Authoring a named check against a single unverifiable historical report violates the evidence-gate discipline documented in [[sources/backlog-disposition-sweep-reduces-accepted-plans-by-half]].

**Fix the pipeline behavior, not the item.** The recurring cost is the terminal state and the noise it deposits into `reports.md`, not this one correction.

## 4. Recommendations (for hngh / hngh-automation)

1. **Add an intake classifier ahead of the matcher.** Route submissions carrying no check identifier and no structured fields into a `non-actionable-operator-note` bucket before they reach matching. This removes the `no named check found` noise for the prose-report class and keeps `reports.md` signal-dense.
2. **Promote the x79 count to a tracked metric.** If the count keeps growing *after* the classifier lands, that growth is the evidence a real `page-rendering-integrity` / `display-content-completeness` named check is warranted — file it then, with reproducible failures attached, not from a prose anecdote.
3. **Run the backlog disposition sweep over the segment containing x79** if it has not already been swept. Per [[sources/backlog-disposition-sweep-reduces-accepted-plans-by-half]], the sweep's evidence gate should independently confirm park-eligibility; if the item already survived a sweep, treat that as confirmation and close.
4. **Verify before acting on root cause.** Read `reports.md:2310` and confirm the record has a null/no-match field rather than a registry-version or sync flag *before* implementing recommendation 1 — so the classifier is aimed at the true cause.

## 5. Open threads (line stays in motion on idle hosts)

- **Read `reports.md:2310`** and settle the coverage-gap vs. registry-sync question (rules in/out the alternative hypothesis). This gates recommendations 1 and 4.
- **Confirm whether the x79 segment has been swept** by the backlog disposition sweep; if yes, close on that confirmation.
- **Watch the x79 metric post-classifier.** Growth is the trigger to file a rendering-integrity named check with reproducible failures; flat/declining count confirms the classifier closed the class.
- **No external sources were verifiable in this transition.** Any claim about matcher internals, registry schema, or `reports.md` structure beyond the supplied metadata is inference and must be re-grounded against the real files before it is treated as fact.

## 6. References

Carried forward as read-only prior art (pointers, not files I opened in this transition):

- [[concepts/moment-of-action-freshness]] — attestation freshness / moment-of-action; grounds why a historical rendering report has no recheckable oracle.
- [[sources/backlog-disposition-sweep-reduces-accepted-plans-by-half]] — evidence-gated disposition sweep; grounds the park decision and the "sweep before/for confirmation" step.
- [[sources/pi-llm-wiki-guardrail-blocks-apply-patch-edits]] — guardrail context for why edits to this record are constrained (carried, not load-bearing here).
- [[sources/async-proof-pattern-for-long-drop-ins]] — background-job proof pattern (contextual; not directly load-bearing on this line).
- [[sources/obs-2026-08-25-night-check-hngh-harness-healthy-timer-doc-vs-install-drift-]] — prior observation of hngh harness doc-vs-install drift; relevant to the registry-sync alternative hypothesis but *not* verified against current state here.

Named repository / location (no specific file paths cited, as I cannot verify their existence or contents in this transition):

- `/home/bricker/Projects/etc/hngh` — hngh kernel repository (named in task; internals unverified).
- `reports.md:2310` — pointer supplied in the alert metadata for the x79 occurrence; **content not independently verified.**

*No external sources were verifiable in this transition. All file-level claims above are limited to what the supplied alert metadata states; the inference tier is flagged as such and must be re-grounded against the real files before any fix ships.*
