# Why did system-awareness set the low-mem headroom flag at 2026-09-14T02:00:16Z (mem >= LOW_MEM_PCT 90, alert identity system-low-mem routed 2026-09-14T02:00:20Z), was it a persistent memory defect or a transient spike, and what disposition (fix or park) does the evidence support?

Status: crystallized 2026-09-15 from research line `fail-20260914-system-low-mem`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-fail-20260914-system-low-mem.md.

# Final structured summary — low-mem headroom flag line

**Line:** Why did system-awareness set the low-mem headroom flag at `2026-09-14T02:00:16Z` (`mem >= LOW_MEM_PCT 90`, alert identity `system-low-mem` routed `2026-09-14T02:00:20Z`), was it a persistent memory defect or a transient spike, and what disposition (fix or park) does the evidence support?

**Lifecycle:** contracting → **crystallized** (this is the line's lasting record; the line stays in motion on idle hosts and may re-open if new evidence lands).

---

## Verification limits (carried forward, still binding)

I cannot read `research-lines.tsv`, the llm-wiki vault, or the hngh kernel tree at `/home/bricker/Projects/etc/hngh` from this environment. **No file path, symbol name, or log line below is asserted as verified.** Where a recommendation names an artifact, it is a *target to confirm on an idle host*, not a confirmed citation. This crystallization therefore distills the expanding-phase hypotheses into concrete, ordered recommendations whose execution steps are expressed as searches/greps rather than claimed paths.

---

## Findings

**F1 — The flag is a threshold crossing, not a named failure.** On the material available to this line, the only established *why* is that `mem` crossed `LOW_MEM_PCT 90` at `02:00:16Z`, and the alert identity `system-low-mem` routed four seconds later at `02:00:20Z`. No deeper root cause (leaking service, unbounded cache, cgroup-limit regression) is named anywhere in the line's record. The question "why did it fire" is therefore only answered to the level of *the comparator fired*; the causal layer is open.

**F2 — Comparator semantics are unconfirmed (H₁), and this gates everything.** The flag name *low-mem headroom* is coherent only if `mem >= LOW_MEM_PCT 90` is a **used/pressure** threshold. Whether `mem` is used-% vs free-%, host-wide vs cgroup-scoped, raw ratio vs PSI pressure has not been established from the comparator source.

[truncated at model call: completion hit the max_tokens cap (finish_reason=length) - re-run the beat]
