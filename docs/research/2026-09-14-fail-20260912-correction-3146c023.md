# Why did clicking "mark read" on the dashboard report queue appear to do nothing for reports (76 aggregated occurrences by 2026-09-12T19:18Z), and what disposition (fix or park) closes it?

Status: crystallized 2026-09-14 from research line `fail-20260912-correction-3146c023`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-fail-20260912-correction-3146c023.md.

# Contracted record — final structured summary

**Line:** Why did clicking "mark read" on the dashboard report queue appear to do nothing for reports (76 aggregated occurrences by 2026-09-12T19:18Z), and what disposition (fix or park) closes it?
**State:** contracting → **contracted** (this is the line's lasting record)

## Direct answer

- **"Why did it appear to do nothing?"** — At the observability level, the click produced no state change that was either *rendered* (frontend local state) or *persisted* (backend). That much is established. **Which of those two is the true cause is not established from repository evidence in this line's record.** No file under `~/Projects/etc/hngh` was read by any beat on this line; every concrete path named in the expanding beat was explicitly flagged as a speculative placeholder, and I cannot verify any hngh path from here. The root cause is therefore narrowed, not proven: it sits among three ranked hypotheses below, with **frontend state-sync failure** as the leading candidate.
- **"What disposition closes it?"** — **FIX, evidence-gated.** Not park. The gate is a two-step localization (reload test + grep) that must land before a concrete patch can be named; the disposition is *fix* because 76 aggregated occurrences of a user-facing silent failure make parking unjustified. See "Disposition" below for the exact close condition and the single falsifier that would reopen it toward park.

## What is established (no code required)

1. **Symptom class = silent UI failure.** The click yields neither a visible change nor a persisted one. That is the whole of what "appeared to do nothing" asserts, and it is consistent with multiple distinct defects.
2. **Recurrence, not a one-off.** 76 aggregated occurrences by 2026-09-12T19:18Z is a systematic signal. This raises the prior on a *state-sync or handler* defect and lowers the prior on transient network flakiness — a flaky link does not aggregate to 76 in one direction (always "nothing," never "wrong value").
3. **The A/B ambiguity is load-bearing.** "Appeared to do nothing" conflates two observably different cases with disjoint fixes and disjoint owners:
   - **(A)** UI never changes — frontend never updates local state (or swallows the error without rollback/feedback).
   - **(B)** UI changes but doesn't survive reload — backend never persists.
   The first diagnostic act is to disambiguate A from B; it needs only a reload or a network-tab look, no code spelunking.

## Root-cause status (epistemic)

**Not established.** Stated plainly so the record does not overclaim: this line has no verified root cause because no hngh source was executed against. The ranked hypotheses are priors, ordered by explanatory power for *both* the silence and the recurrence:

1. **Frontend state-sync failure (leading).** Optimistic update missing, or a `try/catch` that swallows the error with no rollback and no user feedback. Explains both symptoms at lowest cost; highest prior.
2. **Backend handler silent no-op.** Route exists but the status write is dropped — permission check failing closed, an idempotency/duplicate guard treating repeat clicks as no-ops, or a missing transaction commit.
3. **Event/wiring regression.** Button handler detached, or endpoint path drifted (renamed route, versioned API prefix mismatch). Cheap to check; frequently the *actual* cause of "suddenly does nothing."

A fourth angle was present in the expanding beat but is **truncated from the record and not recovered here** — treat it as lost, not as a live hypothesis.

## The single cheapest diagnostic (closes the ambiguity)

Run on a host with repo access; this halves the search space in ≤15 min:

1. **Reload test.** Click "mark read," then hard-reload.
   - Item reappears unread → backend persistence broken → hypotheses 2 or 3.
   - Stays read but the click gave no feedback → frontend-only → hypothesis 1.
2. **Network tab.** Confirm whether a request fires on click and what it returns (2xx + no-op body vs. 4xx/5xx vs. no request at all). "No request" points to wiring (3); "request fires, state reverts" points to sync/rollback (1) or silent no-op (2).
3. **Localize by grep, not assumed layout**, from `~/Projects/etc/hngh`:
   - `grep -rniE 'mark.?read|markRead|mark_read' --include='*.ts' --include='*.tsx' --include='*.js' --include='*.py' --include='*.go' .`
   - `grep -rniE 'reports?/:(id|\{id\})' .` to find the route/handler pair.
   Do **not** trust any path from the expanding beat — they were placeholders.
4. When touching `research-lines.tsv`, use `awk -F'\t'`, not `grep '\t'` (GNU grep treats `\t` as a stray escape and matches nothing).

## Disposition (fix or park)

[truncated at model call: completion hit the max_tokens cap (finish_reason=length) - re-run the beat]
