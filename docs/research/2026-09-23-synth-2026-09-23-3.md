# What is the exact staleness predicate in `fail-20260922-What-is-the-exact-staleness-predicate-in` that distinguishes a stale research line from a parked one under the current kernel ground-truth pass?

Status: crystallized 2026-09-23 from research line `synth-2026-09-23-3`; per-beat
material lives in hngh-automation digest/RESEARCH-BEAT-*-synth-2026-09-23-3.md.

# Research Beat — Final Contraction Record

_line: What is the exact staleness predicate in `fail-20260922-What-is-the-exact-staleness-predicate-in` that distinguishes a stale research line from a parked one under the current kernel ground-truth pass? | state: contracting → crystallized. This record stands; follow-on threads remain in motion on idle hosts._

---

## Scope and confidence boundary

- **Grounded in this line's own material:** the 2026-09-23 prior beat (reproduced above), the vault pointers it cites, the line identifier itself, and the user-named locations `research-lines.tsv` and `[redacted path]
- **Not grounded:** no file inside the hngh kernel repository was read at any point in this line's lifetime. The prior beat states this explicitly, and this beat likewise had no source access. Everything below about the kernel's *implementation* is a hypothesis to be tested, not a finding of fact. No interior kernel file paths are cited for this reason.

## Findings

**F1. The exact implementation-level predicate was never extracted.** Both beats on this line lacked read access to the kernel source. The line closes with the predicate characterized *structurally*, not verbatim. This is the line's central, honest limitation.

**F2. Structural result (the line's real yield): the predicate cannot be a single timestamp comparison; it is necessarily two-channel.** Candidate formalization, carried forward from the prior beat and unverified against source:

- *Staleness channel (evidence-relative):* `stale(L) ⟺ last_verified_output(L) < t_pass ∧ ¬revalidated(L, since t_pass)`, where `t_pass` is the timestamp of the most recent kernel ground-truth pass.
- *Parking channel (intent-recorded):* `parked(L) ⟺ ∃ d ∈ deferrals(L): reason(d) ≠ "stale"`.

**F3. The distinguishing criterion.** Stale is a *relation between a line's evidence and the kernel's current ground truth*; parked is a *declared state with an explicit recorded reason*. The two vary independently: a line may be parked yet fresh, or unparked yet stale. Because

[truncated at model call: completion hit the max_tokens cap (finish_reason=length) - re-run the beat]
