# 20260806 — M9 resume: mandate-to-code sync, plugin wiring, build-fence fix

## What

Resumed the half-completed M9 work after the 20260805 test-loop landing.
Verified the tree first (fast 207/207 after +2, full 1416/1416, lint clean),
then closed three documented gaps.

## Verification baseline (all real runs)

- `make test-fast` — 207/207 checks (hngh-up 44, squad-dispatch 52, beans 62,
  model-runtime 49). EXIT 0.
- `make test-full` — 1416/1416 checks. EXIT 0.
- `make lint-counts` — clean (207 refs match).

## 1. Model mandate (D-040) applied to code

Journal 20260805-model-mandate.md listed code-embedded chains still on the old
kimi/gemma routing. Synced code + prompt-matrix.md together:

- `hngh-up.lisp` — `*model-mapping*` and strategy option labels: budget-50 ->
  deepseek-v4-flash-0731 (was kimi-k2.6 / deepseek-v4-flash), budget-200 ->
  glm-5.2 hermes + deepseek-v4-flash-0731 opencode (was kimi-k3 / gpt-5.6-luna).
- `ai-tool-hub.lisp` — opencode agentic default `-m` -> deepseek/deepseek-v4-flash-0731
  (was local gemma). Test updated in lockstep.
- `squad-resources.lisp` — `*model-vram-mb*` kimi keys -> glm-5.2 / qwen3.7.
- `prompt-matrix.md` — per-role fallback chains, flesh chain, §6.1 role table
  synced to the mandate chain (no kimi, no copilot provider, Artist gets
  qwen3.7-flash vision looker).

## 2. C3 inference bug (found during smoke test)

`answer-from-agents-md` matched the bare phrase "daily driver" and returned
`local-only` against the repo's own mandate-era AGENTS.md, which says "free/local
models are fallbacks, **not** the daily driver". Added negation + remote-primary
branches (budget-50), plus a mandate-phrasing fixture test. The smoke run now
auto-answers `budget-50` with reason "AGENTS.md names a remote primary driver;
locals are fallbacks".

## 3. Wave 2-4 plugins wired into the daemon lifecycle

file-watcher, squad-dispatch, beans, squad-resources were staged in 03282f3
with full tests but never registered in `main.lisp` init/stop/rollback.
Wired all four (init after fragment-journal, reverse-order shutdown in both
chains). squad-startup-automation.md wave table updated 2/3/4 -> done.

## 4. Build-fence bug (found via stale binary)

`LISP_FILES = $(wildcard src/*.lisp src/core/*.lisp)` excluded `src/plugins/`,
so plugin edits never triggered a rebuild — the smoke test ran a stale binary.
Fixed the glob to include `src/plugins/*.lisp`; verified a plugin touch
rebuilds and a no-change run is a no-op.

Attribution: deepseek-v4-flash-0731 via deepseek (Hermes TUI). Uncommitted —
owner review before commit.
