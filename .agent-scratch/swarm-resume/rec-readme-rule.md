# rec-readme-rule: future-records rule in docs/records/README.md

## Governing rule (exact text)

`docs/records/README.md:180-181` (last content bullet before the
"Back to the documentation spine" footer):

```
- Future records name their scope, evidence command, observed result, and
  remaining unknowns.
```

- Line 180: `- Future records name their scope, evidence command, observed result, and`
- Line 181: `  remaining unknowns.` (continuation line)

That is the only explicit future-records rule in the file; no other line
in docs/records/README.md specifies record body structure.

## Adjacent governing statement (record purpose, not body shape)

`docs/records/README.md:3-4` (file preamble):

```
Records preserve verified facts, decisions, and bounded unknowns. They do not
authorize a future action.
```

## docs/README.md context

`docs/README.md:169` describes the tree, no body-structure rule:

```
Evidence, decisions, and the long-form record. Point-in-time only:
```

The docs/README.md (lines 167-182, "Records and history" section) only
declares `records/`, `journal/`, `research/` as the HISTORICAL tree and
links to records/README.md; the body-structure rule lives solely in
docs/records/README.md:180-181.

## Mapping to observed record shape (cross-ref sibling artifact)

Rule terms vs. observed headings: "scope" -> Scope; "evidence command,
observed result" -> Evidence/Result; "remaining unknowns" -> Unknowns.
No rule text mandates a "Title" heading; titles are the dated filename
convention seen across existing records.

## Conclusion

The exact governing text is the two-line bullet at
docs/records/README.md:180-181. Nothing broader restates or extends it.
